# 3Danmu v1.1 → v1.2 回归审计

基线：`v1.1.0` 与上游 `main`（v1.2.0，提交 `34ff1e0e04611077ecd9b203b19f912c199fe460`）。

## v1.1 已经解决的问题

- 修复文件末尾仍反复发 Range 请求的问题；此前可能把异常的小 access unit 送入 MVD，造成系统模块 `svcBreak`。
- 小于 128 字节的异常 H.264 access unit 不再交给 MVD。
- 多 P 选集移入播放器，切 P 时可以保留上屏画面。
- IME 预览字号重新跟随当前字体清晰倍率。

## v1.2 相对 v1.1 的主要改进

- seek 后软件解码画面涂抹：不再对参考帧禁用 H.264 去块滤波，避免 DPB 误差逐帧扩散。
- 线程回收 UAF：统一通过 `thread_reap()` 回收；join 超时只 detach，不再释放仍被线程使用的栈。
- 封面缓存清理卡死：不再一边 `readdir()` 一边删除；改为分批读名、关目录、再删除，并移到低优先级线程。
- 播放地址、列表和 FFmpeg 开流阶段改为临时工作线程，主线程继续绘制并可响应 B 取消。
- 列表加载使用暂存数组，翻页失败或取消不会破坏旧页面；空页被识别为列表结尾。
- seek 重连由一次失败即退出改为有界重试。
- 评论正文容量、折行复杂度、UTF-8 截断和发布时间显示得到修正。
- 记录服务端实际返回的清晰度，撤销大多数视频并不存在的伪 240P 选项。
- Old 3DS 软件解码增加均匀半速策略，改善帧间节奏。
- 设置页改为可滚动列表，后续增加功能不再受六宫格容量限制。

## 本分支额外发现并修复

### 多 P 自动连播实际不会触发

v1.2 的 `main.c` 顺序是：先调用 `player_take_page_pick()`，再调用
`player_ended_naturally()`；但前者会把自然片尾标志清零，所以后一个判断永远为假。

修复方式：自然片尾标志在每次 `player_play()` / `player_play_file()` 入口清零，
取走选集下标时只清选集下标，不再清片尾结果。

### `net_get_img()` 头文件说明与实现相反

实现已经重新使用短请求全局串行锁，但 `net.h` 仍写着“绕过锁、可以并发”。
已修正文档，防止未来维护者按错误契约再次放开 `httpc` 并发。

### 新缓存线程的退出顺序

仅调用 `net_cancel_streams()` 无法取消正在获取播放 URL 的短请求。如果 START 退出时
直接 join worker，可能在超时后拆除 `httpc`，重现 v1.1 类似的后台线程生命周期问题。

修复方式：所有退出路径在收缓存线程前调用 `net_shutdown_begin()`，取消长流和短请求、
拒绝新请求；worker join 完成后才保存数据库和执行 `net_exit()`。

### 入队持久化失败造成“幽灵任务”

任务加入内存后若数据库保存失败，UI 会显示失败，但任务原本仍会被 worker 领取。
现在保存失败会同步回滚内存插入。

### 网络初始化失败后仍调用 B 站接口

上游启动流程即使 `net_init()` 失败也会继续 `load_list()`；空列表按 A 时只重跑
`bili_init()`，没有重新初始化 `httpc`。现在所有 B 站入口都以网络成功标志为门禁；
无网仍可打开并播放已完成缓存，按 A 会重新执行 `net_init()`，成功后才初始化
B 站模块、启动缓存 worker 并加载列表。

## 仍需 3DS 实机验证

- CDN 分别返回 206、忽略 Range 返回 200、URL 中途过期时的 `.part` 行为。
- HOME/睡眠/恢复以及 HOME→X 退出时，缓存 worker 是否都能在限定时间内退出。
- SD 卡满、拔卡、只读和 rename 失败后的数据库/文件对账。
- 本地 MP4 经同一 NetRing、FFmpeg、MVD 管线播放，尤其是 seek 和片尾。
- 大于 2 GiB 文件在当前 devkitARM/newlib 的 `fseeko()`/`off_t` 配置下是否保持 64 位。

本机未安装 devkitARM，因此这些项目不能用 Windows 主机编译替代实机结论。
