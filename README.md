# bilibili for Nintendo 3DS

**v1.5.0**

本项目基于 [OakRoller/3Danmu](https://github.com/OakRoller/3Danmu) 开源代码开发，
保留原项目 MIT 许可证与第三方组件声明。本仓库是非官方社区版本，与哔哩哔哩官方无隶属关系。

基于 3Danmu 的非官方视频客户端,运行在装有自制系统的任天堂 3DS 上。

> **非官方项目,与哔哩哔哩无关,未获其授权或认可。仅供学习交流,严禁用于商业用途。**
> 本项目不分发任何视频内容,不绕过付费/会员/地区限制,不去广告；
> 只缓存用户主动选择的视频或完整分P选集，不提供跨视频自动抓取。
> 使用非官方客户端可能导致账号受限,风险自负。
>
> **与任天堂(Nintendo)亦无任何关联。**「Nintendo 3DS」是任天堂的商标。
> 本项目是运行在自制系统上的第三方程序,不含任何任天堂的代码、密钥或资源。
>
> 许可见 [LICENSE](LICENSE) 与 [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md)。

在一台 2011 年的掌机上刷 B 站:个性化推荐流、搜索(自带拼音输入法)、
扫码登录、在线播放 360P/480P(New 3DS 走 MVD 硬件解码,满帧)、
滚动弹幕、CC 字幕、**裸眼 3D**、历史与收藏、观看进度同步账号。

```
┌─────────────────────────┐
│  上屏 400x240           │   视频画面 + 滚动弹幕
│  (列表页则显示视频列表) │
├─────────────────────────┤
│  下屏 320x240 触控      │   按钮式 GUI(播放控制 / 列表操作 / 设置)
└─────────────────────────┘
```

---

---

## 目录

1. [功能一览](#功能一览)
2. [硬件与系统要求](#硬件与系统要求)
3. [安装使用](#安装使用)
4. [操作说明](#操作说明)
5. [帮忙测试?](#帮忙测试)
6. [已知问题与 TODO](#已知问题与-todo)
7. [致谢与许可](#致谢与许可)

> **帮忙测试?** → **[TESTING.md](TESTING.md)**(不用懂代码,照着做就行)
> **想看实现细节 / 想动手改?** → **[DEVELOPING.md](DEVELOPING.md)**
> (编译方法、架构剖析、以及一份相当长的踩坑记录;
> 下面「[开发者文档](#开发者文档)」一节有索引)


---

## 功能一览

| 功能 | 状态 | 说明 |
|---|---|---|
| 热门视频列表 | ✅ | 滚动到底自动追加，直到接口没有更多内容 |
| 视频搜索 | ✅ | 自动在多条取数路径间回落,提高成功率 |
| 触屏拼音输入法 | ✅ | 自带 QWERTY 键盘 + 5 万词库,任何机型都能打中文 |
| 扫码登录 | ✅ | 上屏显示二维码,手机 B 站 App 扫码;cookie 持久化到 SD 卡 |
| 在线播放 | ✅ | 边下边播,不落地存储 |
| 离线缓存 | ✅ | 单线程后台队列、`.part` 断点续传、播放中入队、分P全集一键入队；按 UP 主文件夹和原视频标题保存 |
| MVD 硬件解码 | ✅ | New 3DS 满帧 30fps,支持含 B 帧的在线流 |
| 软件解码 | ✅ | 全机型可用,约 17-30fps,自适应跳帧追赶音频 |
| 清晰度 360P/480P | ✅ | 480P 需登录,取流失败自动回落 360P |
| 分 P 选集 | ✅ | 主页「查看选集」先展示完整分P，再播放或缓存；播放中也可换 P |
| 倍速播放 | ✅ | 播放中短按 X：1.25x → 1.5x → 2.0x → 1.0x；长按 X 恢复正常 |
| 画面比例 | ✅ | 播放设置里切:自动 / 16:9 / 9:16 / 4:3 / 1:1 / 3:2 / 4:5,当场生效并记住 |
| 滚动弹幕 | ✅ | 后台线程异步加载,zlib 解压 + XML 解析(New 3DS 最多 3000 条) |
| 弹幕覆盖范围 | ✅ | 全屏 / 1/2 / 1/4 / 1/8 屏(从顶部往下算),不想挡脸就往小调 |
| 观看历史同步 | ✅ | 每 15 秒 + 退出时上报进度,手机/网页端可接着看 |
| CC 字幕 | ✅ | 中文轨(人工优先、AI 兜底),用 **bvid** 请求;过长折两行;需登录 |
| 播放设置子页 | ✅ | 3D / 字幕开关 / 弹幕字号 / 字幕字号 / 软解同步模式 / 调试台 |
| 音频播放 | ✅ | AAC → swresample → NDSP,需 dspfirm.cdc |
| 评论区 | ✅ | 播放中下屏浏览,视频不暂停;拖动滚动,到底自动续页、无条数上限 |
| 缓冲态提示 | ✅ | 数据饥饿时冻结时钟 + 显示"缓冲中…",攒够再恢复 |
| 断线自动重连 | ✅ | 后台无限重试(0.5s→3s 退避),连上自动续播 |
| 下屏触控 GUI | ✅ | 列表页 / 播放页 / 设置页三套按钮界面 |
| 竖屏视频 | ✅ | 自适应纹理尺寸后可播放 |
| 进度条拖动跳转 | ✅ | 精确跳转(丢帧对齐到松手位置),可反复拖动不累积偏移 |
| 裸眼 3D 播放 | ✅ | 左右分屏(SBS)片源立体化,弹幕浮在画面前方,深度随 3D 滑块 |
| 列表封面缩略图 | ✅ | 后台线程拉图 + mjpeg 解码,图文卡片列表 |
| 摇杆平滑滚动 | ✅ | 左摇杆像素级滚动列表,十字键选择自动跟随 |
| 首页推荐 | ✅ | 与官方主页同源的个性化推荐流，滚动到底自动追加 |
| 历史记录 / 收藏夹 | ✅ | 需登录 |
| 页面切换 | ✅ | 下屏四按钮:推荐/热门/历史/收藏(当前页高亮),**默认进推荐**;ZL/ZR/SELECT 快切频道 |
| 设置持久化 | ✅ | 弹幕/清晰度/解码方式/字幕/两个字号存 SD 卡,改一项即落盘 |
| 调试日志页 | ✅ | citro2d 自绘(不切屏幕格式),400 行环形缓冲,可拖动滚动 |
| 关注动态 | ❌ | 动态接口 JSON 复杂,二期 |

---


---

## 硬件与系统要求

**必需**

- 3DS / 2DS 系列主机,已安装自制系统(Luma3DS + Homebrew Launcher)
  - 破解教程:https://3ds.hacks.guide(有中文版)
- 浏览和下载时需要可用的 Wi-Fi；已完成缓存的离线播放不需要网络

**强烈建议**

- **New 3DS / New 2DS**:才有 MVD 硬件解码器,能满帧播放 480P
- **老 3DS / 2DS**:没有 MVD,整段靠 ARM11 软解。实测 360P 下解码约
  33ms/帧,而 30fps 的预算是 33ms —— **恰好在能力边缘**,画面约 15~22fps
  且不均匀。播放器会在追不上时自动切成「均匀半速」:帧率更低但节奏稳定,
  比忽快忽慢好看。声音始终流畅
- **DSP 固件**:Rosalina 菜单(**L + 下 + SELECT**)→ Miscellaneous options
  → Dump DSP firmware,看到 `successfully written` 即可,
  生成 `sdmc:/3ds/dspfirm.cdc`。**没有它完全没有声音**
  (画面一切正常,所以很容易被误当成 bug)

**机型默认设置**(可在设置页改)

| 机型 | 清晰度 | 解码 |
|---|---|---|
| New 3DS / New 2DS | 480P | MVD 硬解 |
| 老 3DS / 2DS | 360P | 软解 |

---


---

## 安装使用

1. 把 `bilibili.3dsx` 拷到 SD 卡的 **`/3ds/`** 目录(不是根目录);
   放子目录 `/3ds/3danmu/bilibili.3dsx` 也可以,HBL 同样能扫到。
   字体和拼音词库都打包在 .3dsx 内(romfs),不需要额外拷文件
2. **dump DSP 固件**:Rosalina 菜单(**L + 下 + SELECT**)→
   Miscellaneous options → Dump DSP firmware,看到 `successfully written`
   即生成 `sdmc:/3ds/dspfirm.cdc` —— **没有它完全没有声音**
3. SD 卡插回主机,连好 Wi-Fi
4. 打开 Homebrew Launcher,选择 bilibili 启动
   (想要免开 HBL、直接出现在主菜单,见 [DEVELOPING.md 的「打包成独立应用」](DEVELOPING.md#从源码编译),打好的 cia 用 FBI 安装)

**开发调试强烈建议用 `3dslink` 无线推送**(免插拔 SD 卡):
主机 HBL 里按 Y 进 netloader,电脑上 `3dslink bilibili.3dsx`,
程序经 Wi-Fi 推过去立即运行。自动发现失灵时用 netloader 界面显示的 IP:
`3dslink -a 192.168.x.x bilibili.3dsx`。注意推送运行的是内存副本,SD 卡上的旧文件
不会被更新,调稳定后记得拷一次卡。传其他文件(cia、字体)可以在 3DS 上
装个 ftpd,电脑用任意 FTP 客户端连,同样免插卡。

登录信息存在 `sdmc:/3ds/3danmu/cookies.txt`,下次启动自动恢复。

> ⚠️ `cookies.txt` 里是**有效的账号凭证**。发日志、发截图、把 SD 卡借给别人
> 之前记得处理掉,和密码同等对待。

### 万一登录后发不了弹幕

正常情况下扫码登录就够了,全部功能可用。但如果发弹幕时提示缺 `bili_jct`,
说明登录走的是**降级路径**(原因见
[DEVELOPING.md 的「为什么有个 tls.c」](DEVELOPING.md#为什么有个-tlsc))。
手动补一次即可,**做一次就长期有效**:

1. 电脑浏览器登录 bilibili.com
2. F12 → Application(应用) → Cookies → `https://www.bilibili.com`
3. 找到 `bili_jct`,复制它的值(32 位十六进制)
4. 编辑 SD 卡上的 `sdmc:/3ds/3danmu/cookies.txt`,加一行 `bili_jct=你复制的那32位`
5. 重启 bilibili

同一个位置的 `SESSDATA` 也能这样导入,两个都填就等于登录,连扫码都省了。

---

## 操作说明

### 下屏触控 GUI(主要交互方式)

**列表页**

```
┌──────────────────────────────────┐
│ 已登录 / 未登录                  │
│ [ 推荐 ] [ 热门 ] [ 搜索 ]       │
│ [ 历史 ] [ 收藏 ] [ 设置 ]       │
│ [登录/注销] [缓存当前] [查看选集]│
│ [          离线下载任务         ]│
│  [状态条:进度 / 按键提示]       │
└──────────────────────────────────┘
(实体 Y 仍可搜索；推荐、热门、历史滚动到底自动加载，当前频道按钮高亮)
```

**离线缓存**:在上屏列表选中视频后点「缓存当前」，或在播放页点
「缓存视频/缓存本P」，任务会进入单线程后台队列。相同 `bvid + cid` 已经
完成、正在下载或已经排队时只显示状态提示，不会重复加入。点
点「查看选集」会先显示真实的完整分P列表；确认内容后再点「缓存完整列表」，
即可一次加入全部分P。点「离线下载任务」后，十字键选择任务，
`A` 播放已完成缓存，`X` 暂停/继续/重试，`Y` 删除，`B` 返回。在线播放时
后台下载会自动让路，退出播放后从 `.part` 续传。文件保存在：

```text
sdmc:/3ds/3danmu/cache/videos/<UP主名>/<原视频标题>.mp4
```

同名冲突才会追加 `[bvid-cid-qn]`；不合法的 FAT 字符会替换为 `_`。

**选集**(仅多 P 视频):点进去**直接播第一 P**,不再先问一遍 ——
点进合集就想从头看是最常见的意图,开播前横插一页只是摩擦。
要换 P 时,播放页下屏左下角的按钮是「选集」而不是「返回」
(返回本来就有 B 键,而选集在播放中没有别的入口)。
主页也可先点「查看选集」浏览完整列表、播放指定分P或缓存完整列表。
播放中点开选集后上屏继续播放,下屏换成列表,点哪一 P 换哪一 P；顶部可点「缓存全集」；
点当前这一 P 或按 B 就关掉,继续看。

**播放页**:标题、播放时间 / 总时长、当前解码方式,
返回(多 P 视频显示为**选集**)/ 暂停·播放 / **设置** / 弹幕开关 /
**缓存视频或本P** / 评论按钮
(**播放设置子页**收纳:3D 开关、CC 字幕开关、弹幕字号、字幕字号(均小/中/大)、
**画面比例**、**弹幕范围**、调试台入口,右下角软解时是同步·流畅模式、
硬解时是返回键(该模式对硬解无意义),
并实时显示字幕加载状态。B 键随时退出设置),
以及底部的**可拖动进度条**。

- **画面比例**:自动(片源原始比例)/ 16:9 / 9:16 / 4:3 / 1:1 / 3:2 / 4:5。
  选「自动」以外的档位是**拉伸**到该比例,不是裁切 —— 想去掉上下黑边、
  或者把竖屏片撑大一点时用。改一下当场生效,并且会记住
- **弹幕范围**:全屏 / 1/2 / 1/4 / 1/8(都从上屏顶部往下算)。
  实际剩几行由「范围 x 弹幕字号」共同决定,切换时调试台会打印行数

进度条用法:按住把手左右拖动,拖动时上方气泡显示目标时间;
**松手才真正跳转**,跳转后会短暂显示"缓冲中"。
(上屏原有一条细进度条,后已去掉——下屏控制条足够,画面留给视频和弹幕。)
命中区上下各放宽了 14 像素,手指也能操作。

**设置页**:

- **弹幕:开/关** — 是否加载并显示弹幕
- **清晰度:360P/480P** — 480P 需登录,取流失败自动回落
- **解码:自动/软解** — 自动 = New 3DS 优先硬解;软解 = 强制 ffmpeg 软解
- **调试台** — 切到全屏日志控制台(点触摸屏返回)
- **返回**

### 实体按键

| 按键 | 列表页 | 播放中 |
|---|---|---|
| 十字键 上/下 | 选择视频 | — |
| A | 播放 / 加载失败时重试 | 暂停·继续 |
| B | 从任意页面返回推荐页 | 退出播放 |
| X | —（登录/注销只用下屏按钮） | 短按循环 1.25x / 1.5x / 2.0x / 1.0x；长按恢复 1.0x |
| 触摸拖动 | 点按钮 | 拖进度条跳转 |
| Y | 搜索 | — |
| L / R | 向上翻一屏 / 手动加载下一批（也会自动加载） | — |
| ZL / ZR / SELECT | 切换 推荐↔热门 | — |
| START | 退出程序 | — |

### 调试台

任意「设置」页 → 点「调试台」→ 手指**拖动滚动**、**双击**退出。
上下键也能滚。

它记录最近 400 行:网络请求、解码器状态、MVD 返回码等等。
**打开之前发生的事也都还在**,所以出问题之后再打开来得及 ——
排障最需要的往往正是那段历史。

出问题时把这一页拍下来,比任何文字描述都有用。

> 调试台输出**全是英文/ASCII**。动态内容(视频标题、接口返回的中文
> 错误信息)会被过滤成 `?`,所以看到 `>> ??????` 是正常的,那是中文标题。
> 上屏会显示完整中文。

日志页是 citro2d 自绘的,支持中文界面、触摸拖动、滚动条。
为什么不用 3DS 自带 console、以及这套日志踩过哪些坑,见
[DEVELOPING.md 的「调试方法」](DEVELOPING.md#调试方法)。

### 同步优先 vs 流畅优先(仅软解有意义)

软解速度不足时,播放器要在"音画同步"和"帧率"之间取舍:

- **流畅优先**(默认):落后音频 >400ms 才跳帧,画面延迟约 0.2-0.4 秒但更流畅
- **同步优先**:落后 >150ms 就跳帧,嘴型对得更准但跳帧更频繁

硬解速度远超需求,两种模式无差别。

---

## 帮忙测试?

有一份专门写给测试者的文档:**[TESTING.md](TESTING.md)** ——
装程序、dump 音频固件、逐项测什么、怎么判断正常与否、怎么反馈。
**不用懂代码**,照着做就行。

## 已知问题与 TODO

### 已知问题

- **搜索结果结构会变**。服务端返回的结构随版本变化,代码里按几种已知形状
  依次尝试;若某天全部落空,调试台会打印响应头部便于定位新结构。
  这类适配随时可能失效,属于第三方客户端的常态
- ~~退出播放时可能有短暂延迟~~ **v1.0.0 已修**:退出/关闭时主动
  `httpcCancelConnection` 掐掉在途请求,各线程 join 压到 0.5 秒,
  实测全链路 190ms(修复过程见「3DS 开发踩坑记录」的 Closing software 条)
- **CDN 链接有时效**。断网超过一两小时后,即使重连也拉不到数据,
  退出重进即可
- **首次加载封面慢(约十几秒/页)**。3DS 网络栈单次往返就要几百毫秒,
  而每页 20 张图;实测总字节数才 46KB,即纯延迟受限。
  已确认**加线程、加并发、调优先级全部无效**(httpc 服务内部本就串行),
  唯一有效的是 SD 卡缓存 —— 所以"再看一次"是秒出的,"第一次"没救
- **字幕的首选取数路径长期不可用**,已自动降级到备用路径,
  **字幕功能不受影响**;首选路径哪天恢复会自动切回去

### TODO(欢迎继续)

- [ ] **竖屏模式**:检测到竖屏视频(vh > vw)时自动把画面和 GUI 整体旋转 90°,
      这样可以把 3DS 竖过来当手机看,视频能占满整个上屏而不是中间一条窄带。
      实现要点:视频纹理绘制时用 `C2D_DrawImageAt` 的 angle 参数旋转;
      下屏 GUI 的按钮布局与触控坐标同步做 90° 变换(可在 `ui_button` 里
      加一层坐标映射);进度条改为竖向或保留在屏幕一侧;
      弹幕滚动方向也要跟着转(或竖屏时改为从上往下滚)
- [ ] 关注动态(用 x/polymer/web-dynamic 接口,JSON 嵌套很深)
- [ ] 收藏夹切换(目前只读默认收藏夹;folder/created/list-all 已返回全部夹列表)
- [ ] 弹幕高级功能:顶部/底部固定弹幕、屏蔽词、透明度调节
      (**覆盖范围**已做,见播放设置子页)
- [ ] 3D 模式自动检测(标题/标签含"3D""左右"时自动开)、上下分屏(TB)格式支持
- [ ] **720P 3D(实验性)**:720P 整帧 1280 宽超出 3DS 纹理上限(1024),
      需改成分眼双纹理管线——解码后 CPU 把左右两半(各 640x720)分别拷进
      两张纹理。仅对 SBS 3D 可行(2D 720P 需要拼接纹理,不做)。
      已核过的账:MVD 硬件可解 720P30;qn=64 登录后有 mp4 直链;
      码率约 200-300KB/s 带宽够。两个风险:linear 堆余量
      (双纹理 4MB + MVD 双缓冲 3.7MB,总堆 32MB 已用约 10MB)、
      每帧 1.8MB 的分眼拷贝对主线程的压力(可能要挪 worker)。
      仅 New 3DS + 3D 模式开放,失败回落 480P
- [x] ~~分 P 视频支持(目前只播第一 P)~~ **已做**:开播前的选集页,
      每一 P 用自己的 cid(弹幕/字幕/进度上报都跟着换)。
      剩下的小尾巴:合集(ugc_season)还没做,那是**跨稿件**的列表,
      和分 P 不是一回事
- [ ] 点赞 / 投币 / 收藏(POST 通路已打通,照着发弹幕写即可)

---


---

## 致谢与许可

这个项目站在很多人的肩膀上。下面按「帮了多大忙」排,不是按字母序 ——
标 ⭐ 的那几个,没有它们本项目做不出来。

**打通硬解的关键**

- ⭐ [Core-2-Extreme/Video_player_for_3DS](https://github.com/Core-2-Extreme/Video_player_for_3DS)
  —— CIA 下 MVD 硬解卡了很久,最后是读它的 `app.rsf` 才发现
  exheader 少了 `Dependency: mvd`。**没有这个参照,硬解大概到现在还是死路**
- ⭐ [3dbrew](https://www.3dbrew.org) —— MVD 服务文档。
  「内核版本必须 >= 2.44」这一句救了最后一个卡关的 bug
- [devkitPro](https://devkitpro.org) —— 工具链、libctru、citro2d/citro3d、
  以及 `3ds-examples` 里的 mvd 例程(它证伪了我一个错误猜测)

**接口这一侧**

- SocialSisterYi 及 bilibili-API-collect 的众多贡献者 —— 早期开发阶段的
  重要参考。该项目已于 2026 年 1 月停止维护并关闭,谨在此致谢

**字体与中文**

- ⭐ [cadl/ClouDS-Music](https://github.com/cadl/ClouDS-Music)(MIT)
  —— 同为 3DS 上的第三方客户端,它的 `normalize_bcfnt.py` 让我知道了
  **mkbcfnt 不初始化 FINF 默认字宽**这个坑(缺字处宽度乱跳、和相邻字重叠)。
  `tools/bcfnt_fix.py` 是按同一思路独立重写的
- [Noto Sans CJK](https://github.com/notofonts/noto-cjk)(SIL OFL 1.1)—— 界面中文字体
- [jieba](https://github.com/fxsjy/jieba)(MIT)与
  [pypinyin](https://github.com/mozillazg/python-pinyin)(MIT)
  —— 拼音输入法的词频与读音数据来源

**直接用到的代码**

- [jsmn](https://github.com/zserge/jsmn)(Serge Zaitsev,MIT)—— JSON 分词
- [QR-Code-generator](https://github.com/nayuki/QR-Code-generator)(Project Nayuki,MIT)
  —— 登录二维码
- [FFmpeg](https://ffmpeg.org/)(LGPL 2.1+)—— 解封装、软解、重采样、缩放

**还有**

- Luma3DS 与 Rosalina —— 崩溃页上的 `Current process` / `PC` / `FAR`
  配 `addr2line` 就能定位到源码行,这个项目一半的 bug 是这么破的
- ftpd —— 无线传 CIA 和拉 `trace.log`,省下无数次插拔 SD 卡

本项目仅供个人学习与技术研究使用。请遵守哔哩哔哩用户协议,
不要用于商业用途或滥用接口。与哔哩哔哩官方无任何关联。

代码许可见 [LICENSE](LICENSE)(MIT);二进制中第三方组件的许可与义务
(FFmpeg 的 LGPL、字体的 OFL 等)见
[THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md) ——
**其中若干义务是「谁分发谁承担」的,转发构建产物前请先读一遍。**


---

## 开发者文档

技术内容都在 **[DEVELOPING.md](DEVELOPING.md)**。挑几个可能对你有用的:

**如果你也在给 3DS 写东西**(这些和 B 站无关,是通用经验):

- [CIA 想用 MVD 硬解:四个 exheader 条件缺一不可](DEVELOPING.md#3ds-开发踩坑记录) ——
  缺哪个的症状完全不同(IPC 无限挂起 / init 假装成功后崩 / data abort),
  单独排查很难对上号
- [HOME→X 卡在 "Closing software":五层等待的总和](DEVELOPING.md#3ds-开发踩坑记录) ——
  修一层露一层,最后一层是 APT 钩子的竞态
- [libctru 的 `mvdstdProcessVideoFrame` 少转了一次地址窗口](DEVELOPING.md#3ds-开发踩坑记录) ——
  崩溃页的 FAR 恰好等于自己那块缓冲的地址
- [位图字体只有一个"清晰"字号](DEVELOPING.md#中文字体) ——
  以及为什么点阵字体在 12px 汉字上是条死路(附纹理转储)
- [httpc 的请求既没有超时,也不认标志位](DEVELOPING.md#3ds-开发踩坑记录) ——
  唯一的打断手段,以及它带来的两个新坑

**如果你想改这个程序**:

- [从源码编译](DEVELOPING.md#从源码编译) —— devkitPro、交叉编译 ffmpeg、打包 CIA
- [代码结构](DEVELOPING.md#代码结构) 与 [技术架构详解](DEVELOPING.md#技术架构详解) ——
  三线程流水线、音画同步、GPU 合成、内存预算
- [两条从网络层学到的通用经验](DEVELOPING.md#两条从网络层学到的通用经验) ——
  代际计数里「跳过」与「作废」的区别;把假设编码进程序让它自己做实验
- [调试方法](DEVELOPING.md#调试方法) —— 调试台、trace.log、崩溃页配 addr2line 定位到源码行
