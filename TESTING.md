# 3Danmu 测试指南

- [0. 准备](#0-准备) —— 装程序、dump 音频固件、登录
- [1. 怎么反馈](#1-怎么反馈--先学会看调试台) —— 先学会看调试台
- [2. 每一项测什么](#2-每一项测什么怎么判断) —— 11 个测试项,含本轮重点
- [3. 已知的正常现象](#3-已知的正常现象不用报) —— 这些不用报
- [4. 反馈模板](#4-反馈模板)
- [测完之后](#测完之后)

---

这份文档是全部你需要知道的。**不用懂代码**,照着做、
把看到的现象说出来就行。

> 3Danmu 是运行在自制系统 3DS 上的非官方视频客户端。
> 程序本身的介绍见 [README.md](README.md);
> 想看实现细节见 [DEVELOPING.md](DEVELOPING.md)。

## 0. 准备

| | |
|---|---|
| 机器 | New 3DS / New 2DS 系列(旧机型能跑但会慢,且只开 1 条封面加载线程) |
| 系统 | 自制系统(Luma3DS 等) |
| 网络 | Wi-Fi 连好 |

### 装程序:`.3dsx` 放哪

把 `3danmu.3dsx` 放到 SD 卡的 **`/3ds/`** 目录下(不是根目录):

```
SD 卡根目录/
└── 3ds/
    └── 3danmu.3dsx
```

然后开机进 **Homebrew Launcher**,列表里就有 3Danmu。

- 中文字体、拼音词库都**打包在 .3dsx 里面**了,不需要额外拷文件
- 也可以放子目录 `/3ds/3danmu/3danmu.3dsx`,HBL 同样能扫到
- 如果拿到的是 `.cia`:用 FBI 安装,之后从 3DS 主菜单直接点图标进,
  不用每次开 Homebrew Launcher

### 要声音:必须先 dump `dspfirm.cdc`

**没有这个文件就完全没有声音**,而画面一切正常 —— 这是最常被误报成
"没声音的 bug"的情况。它是 3DS 的 DSP 固件,受版权保护不能分发,
必须**从自己的机器里导出**。一台机器做一次就够了。

**Luma3DS 自带这个功能,不用下载任何东西**:

1. 在主菜单(或任意界面)按 **L + 下 + SELECT** 呼出 **Rosalina** 菜单
2. 选 **Miscellaneous options**
3. 选 **Dump DSP firmware**
4. 看到 **`successfully written`** 就成功了
5. 按 B 一路退出

它会生成 **`sdmc:/3ds/dspfirm.cdc`**。做一次就够,以后都不用管。

装完之后目录长这样:

```
SD 卡根目录/
└── 3ds/
    ├── dspfirm.cdc        ← Rosalina 生成的,有它才有声音
    └── 3danmu.3dsx
```

> 如果播放时画面正常但一点声音都没有,先回来检查这个文件。
> 调试台里会打印 `audio unavailable (missing dspfirm.cdc?), video only`。

### 登录

历史、收藏、字幕、480P 都需要登录。下屏点"扫码登录",
用手机 B 站 App 扫上屏二维码。登录态存在 SD 卡
(`sdmc:/3ds/3danmu/cookies.txt`),下次自动恢复。

## 1. 怎么反馈 —— 先学会看调试台

**这是最重要的一步。** 光说"卡了"我没法定位,但日志里往往直接写着原因。

任意"设置"页 → 点"调试台" → **手指拖动**往回翻 → **双击**退出。

日志是 400 行环形缓冲,**打开调试台之前发生的事也都留着**,
所以出问题后再打开也来得及。拍照发我就行。

## 2. 每一项测什么、怎么判断

下面每一项都写清了「怎么操作」和「什么算正常 / 什么算有问题」。
**有问题的那一项,请一定配上调试台截图。**

### 2.1 列表与滚动

- 开机进"推荐",用**摇杆**上下滚,再用**十字键**上下选
- 长按十字键不放,看连续滚动
- ZL/ZR 切换 热门/推荐；推荐、热门、历史向下滚到底应自动追加

✅ 正常:滚动始终跟手、不掉帧;封面逐张浮现;选中的长标题会缓慢跑马灯

❌ 报给我:滚动一顿一顿 / 偶尔卡一下 / 文字或画面出现乱码方块、拉伸色带

### 2.2 封面加载速度

SD 卡缓存的回归验证:

1. 进列表,**等封面全部加载完**(下面写"加载封面 20/20")
2. 按 R 翻到下一页,等它也加载完
3. 按 L 翻回上一页

✅ 正常:第 3 步**几乎瞬间出图**。调试台里能看到
`thumb: 20 imgs in ...ms (cache 20/20, ...)` —— `cache 20/20` 表示全部命中缓存

❌ 报给我:翻回来仍然要等好几秒,或 `cache 0/20`

> 第一次见到的封面依然要走网络、要十几秒,**这是正常的**,改不了 ——
> 3DS 网络栈单次往返就要几百毫秒,而每页有 20 张图。
> 缓存能解决的是"再看一次",不是"第一次"。

顺带试试:**设置 → 清理封面缓存**(按钮上显示当前占用),点完再进列表,
应该又变回第一次那样慢 —— 这说明缓存确实在起作用。

### 2.3 播放:流畅度与音画同步

挑一个**十分钟以上**的视频,完整看几分钟,别只看开头。

✅ 正常:画面流畅;口型和声音对得上;弹幕匀速飘过

❌ 报给我:
- **画面卡一下但声音正常** → 请务必看调试台,找 `main loop blocked` 或 `stall`
- 音画越看越对不上(前面对、后面偏)
- 弹幕一顿一顿地跳

> 判断技巧:**画面和弹幕是否同时停**。同时停 = 主线程被卡住;
> 只有画面停 = 解码或网络。这一句话能帮我省掉好几轮排查。

### 2.4 播放:跳转

拖下屏进度条,**前后各拖几次**,快速连拖也试试。

✅ 正常:松手后画面直接跳到目标位置;进度条不回弹;音画依然同步

❌ 报给我:进度条松手后往回跳一下 / 跳转后黑屏 / 跳转后音画错开 / 花屏

### 2.5 字幕

需要登录。播放中 → 下屏"设置" → 打开"字幕"。

1. 找**有 AI 字幕**的视频(大多数中文长视频都有)
2. 打开字幕后**等 10-30 秒**
3. 重点:**连续切换视频** —— 上一个的字幕还在转圈就退出去点下一个,来回几次

✅ 正常:字幕内容和这个视频对得上;切换视频后字幕跟着换

❌ 报给我:
- **字幕内容是别的视频的** → 立刻看调试台,找 `sub:` 开头的行
- 明明有字幕却一直不出来(超过 30 秒)

> 调试台里 `subtitle try1:` ~ `try20:` 表示第几次尝试。
> AI 字幕是服务端按需生成的,前几次查不到很正常。
> 如果经常要到 `try12` 往后才出来,告诉我,我把重试排得更密。

### 2.6 弹幕、缓存与选集

- 弹幕开关、字号(设置里)
- 缓存按钮:播放中应位于原“发弹幕”按钮位置，不能遮住进度条
- 主页点“查看选集”:上屏必须出现完整分P列表；进入后才显示“缓存完整列表”
- 已登录时点“注销”:必须先出现确认弹窗，取消后账号仍保持登录

✅ 正常:选集可浏览和选择；全集只入队一次；拖进度条不会点到缓存按钮

❌ 报给我:选集空白 / 重复缓存 / 缓存按钮挡住或误触进度条 / 点注销直接退出

### 2.7 评论区

播放中 → 下屏**右下角「评论」** → 下屏变成评论列表,**上屏视频照常播**。

- 手指拖动滚动;**滚到底自动加载下一页,没有翻页按钮、没有条数上限**
- 按 B 退出(底部有提示行)

✅ 正常:评论内容和这个视频对得上;滚动跟手;一路滚到几百条也不断页

❌ 报给我:打开评论的瞬间画面或弹幕卡住(**这说明网络请求跑到主线程上了**) /
评论是别的视频的 / 长评论显示不全或和相邻评论叠在一起

### 2.8 裸眼 3D(有 3D 屏的机器)

播放中 → 设置 → 打开 3D → 推 3D 滑块。左右分屏(SBS)片源效果最好。

✅ 正常:有立体感;滑块能调深浅;推满时轻微重影属正常

❌ 报给我:左右眼反了(看着凹进去) / 画面撕裂

### 2.9 长时间稳定性

**这一项最有价值,也最容易被跳过。** 连续用二三十分钟:
反复进出播放、翻页、切频道、搜索。

❌ 报给我:突然所有网络请求都失败 / 越用越慢 / 崩溃(黑屏或 Luma 报错页,
请把 Luma 那页拍下来,上面有 `Current process`)

### 2.10 CIA 硬解确认(**本轮重点**)

装的是 `.cia` 的话,硬解曾经是完全跑不通的(exheader 差四个条件,
v1.0.0 才修齐)。请确认:

1. 随便播一个 480P 视频
2. 打开调试台,找 `decoder:` 那一行

✅ 正常:`decoder: MVD (hardware)`,画面满帧

❌ 报给我:`decoder: software - mvdstdInit failed`(附上前后几行,
尤其 `mvd:` 开头的);或者整机死机/重启(拍 Luma 报错页)

### 2.11 关闭与退出(**本轮重点**)

这一项修了五层叠加的等待,请多做几遍:

- **播放中** HOME → X 关闭
- **列表加载封面时** HOME → X 关闭
- 播放中按 B 退到列表,再按 START 退出程序
- HOME 挂起后再点回来继续看

✅ 正常:每种关法都在一两秒内回到主菜单;挂起回来能继续播

❌ 报给我:卡在 "Closing software" 超过五秒(这最重要 ——
重进后把调试台或 `sdmc:/3ds/3danmu/trace.log` 的**最后十行**发我,
最后一行卡在哪一步,问题就在哪一段)

## 3. 已知的正常现象(不用报)

- 开播前短暂显示"载入弹幕…" —— 在等弹幕就绪,为的是不漏掉开头的弹幕
- 第一次进某一页,封面要十几秒才陆续出齐(见 2.2)
- 调试台里这些都是正常的:
  - `mvdstdInit=00000000` —— 跳转时重开硬解码器
  - `webview cid failed, trying pagelist` —— 接口风控,自动兜底
  - `sub: tryN failed` —— 首选取数路径被拒,自动走备用路径,
    **不影响字幕功能**
  - `pts drift: trimmed N` —— 时间戳自校正在工作
- 某些视频提示"无法播放" —— 多为已下架或需大会员,上屏会显示 API 给的原因
- 封面下载中按 HOME 偶尔要等一下下 —— 在途的那一个请求已被主动掐断,
  等待只剩毫秒级;之前挂着的旧封面恢复后会自动补
- 调试台里 `httpc sick but stream busy, skip reset` —— 自愈机制在避让
  正在工作的下载线程,行为正确

## 4. 反馈模板

照这个格式说,我基本能直接定位:

```
机器:New 3DS / 旧 3DS
现象:(具体做了什么 → 看到了什么)
频率:每次都有 / 偶尔 / 只出现过一次
调试台:(截图,或念出相关的几行)
```

**比"卡"更有用的描述**:什么时候卡、卡多久、卡的时候声音正不正常、
弹幕停没停、之前做了什么操作。这些细节每一条都能砍掉一半排查范围。

**看到 Luma 红字报错页**:整页拍下来(`Current process`、`PC`、`LR`、
`FAR` 都有用)。给开发者的提醒:每次刷机后把当时的 `3danmu.elf` 留一份,
拿崩溃页上的 PC 跑 `arm-none-eabi-addr2line -e 3danmu.elf -f -C 0x地址`
能直接定位到源码行 —— 实测一条崩溃链五分钟破案,前提是 elf 和装机的
是同一次构建。

---


---

---

## 测完之后

反馈发给维护者即可(GitHub Issue 或你们约定的渠道)。

**最有价值的三种反馈**,按顺序:

1. **崩溃** —— Luma 的红字报错页整页拍下来(`Current process`、`PC`、
   `LR`、`FAR` 都有用)
2. **卡死** —— 卡住的场景 + 重启后 `sdmc:/3ds/3danmu/trace.log` 的最后十几行
3. **稳定复现的功能问题** —— 操作步骤 + 调试台截图

「偶尔有点卡」这种最难处理,但如果你能说出**卡的时候声音正不正常、
弹幕停没停**,它立刻就变得可查了。

谢谢你花时间。
