# bilibili（基于 3Danmu）开发者文档

本文是 bilibili（基于 3Danmu）的技术文档:怎么编、怎么改、以及踩过的坑。
面向使用者的说明(安装、操作)在 **[README.md](README.md)**,
帮忙测试的指南在 **[TESTING.md](TESTING.md)**。

> **非官方项目,与哔哩哔哩无关,与任天堂(Nintendo)亦无任何关联。**
> 仅供学习交流,严禁用于商业用途。不含任何任天堂的代码、密钥或资源。
> 许可与第三方组件义务见 [LICENSE](LICENSE) 和
> [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md)。

> 其中「[3DS 开发踩坑记录](#3ds-开发踩坑记录)」和
> 「[MVD 硬件解码完全指南](#mvd-硬件解码完全指南)」两节**与 B 站无关**,
> 对任何 3DS 自制程序都适用 —— 如果你是搜某个具体错误来的,大概在那里。

---

## 目录

1. [从源码编译](#从源码编译)
2. [发布流程](#发布流程)
3. [代码结构](#代码结构)
4. [技术架构详解](#技术架构详解)
5. [列表封面与滚动](#列表封面与滚动)
6. [触屏拼音输入法](#触屏拼音输入法)
7. [中文字体](#中文字体)
8. [MVD 硬件解码完全指南](#mvd-硬件解码完全指南)
9. [3DS 开发踩坑记录](#3ds-开发踩坑记录)
10. [两条从网络层学到的通用经验](#两条从网络层学到的通用经验)
11. [关于网络层与接口](#关于网络层与接口)
12. [调试方法](#调试方法)


---

## 从源码编译

### 1. 安装 devkitPro

macOS:

```bash
xcode-select --install
# 从 https://github.com/devkitPro/pacman/releases 下载 pkg 安装器并安装
# 安装后重启,让环境变量生效
```

Linux / Windows(MSYS2)见 https://devkitpro.org/wiki/Getting_Started。

### 2. 安装工具链和依赖

```bash
sudo dkp-pacman -S 3ds-dev 3ds-pkg-config 3ds-zlib
```

> **国内网络注意**:`pkg.devkitpro.org` 经常返回 403。解决办法:
> - 挂代理:`sudo env http_proxy=http://127.0.0.1:7890 https_proxy=http://127.0.0.1:7890 dkp-pacman -S ...`
> - 或浏览器手动下载 `.pkg.tar.zst` 和对应的 `.sig`,放进
>   `/opt/devkitpro/pacman/var/cache/pacman/pkg/` 后再执行安装命令
> - 或本地直装:`sudo dkp-pacman -U /path/to/xxx.pkg.tar.zst`

### 3. 交叉编译 ffmpeg

devkitPro 官方 portlibs **没有** 3DS 版 ffmpeg(只有 Switch 版),需要自己编译:

```bash
./build-ffmpeg-3ds.sh
```

脚本做的事:下载 ffmpeg 6.1.2 源码 → 按 3DS 交叉编译配置 configure
(只启用 h264/aac 解码、mov/flv 解封装、swresample、swscale,关掉一切用不到的)
→ 编译 → 安装到 `$DEVKITPRO/portlibs/3ds`。约需几分钟。

如果 ffmpeg.org 下载慢,手动下载 `ffmpeg-6.1.2.tar.xz` 放到工程根目录再跑脚本。

> 脚本里加了 `-Wno-error=incompatible-pointer-types` 等三个降级参数,
> 因为新版 GCC(devkitARM 的 gcc 16)会把 newlib 的 `int32_t` = `long`
> 与 ffmpeg 期望的 `int` 之间的良性不匹配当成错误。

### 4. 编译本项目

```bash
export DEVKITPRO=/opt/devkitpro
export DEVKITARM=$DEVKITPRO/devkitARM
make          # 增量编译
make clean && make   # 完全重编
```

产物:`bilibili.3dsx`。

> **重要**:工程路径中**不能有空格**(devkitPro 的 Makefile 不支持)。
> 例如放在 `~/3danmu` 而不是 `~/My Projects/3danmu`。

### 5. PC 端单元测试(可选)

纯逻辑模块可以在电脑上直接测:

```bash
gcc -Wall -Wextra -O1 -o /tmp/t test/host_test.c source/md5.c source/wbi.c source/jsonx.c && /tmp/t
```

覆盖 MD5(含跨块边界)、请求签名(与参考实现对拍)、
JSON 路径访问与 UTF-8 反转义(含缓冲区边界回归测试)。

### 6. 打包成独立应用(CIA,免开 Homebrew Launcher)

`.3dsx` 要通过 Homebrew Launcher 启动;打包成 `.cia` 安装后,
图标直接出现在**主菜单**,和正式软件一样点开就用(需要自制系统,即 Luma)。

**安装打包工具**(一次性):

```bash
# makerom:从 https://github.com/3DSGuy/Project_CTR/releases
# 下载 macos 版,解压后:
chmod +x makerom && sudo mv makerom /usr/local/bin/

# bannertool:原仓库(Steveice10)已被删除,用社区维护的分支。
# 注意它已改用 CMake,不是 make;Releases 只有 Windows/Linux 预编译版,
# macOS 必须自己编:
brew install cmake
git clone https://github.com/carstene1ns/3ds-bannertool
cd 3ds-bannertool
cmake -B build -DCMAKE_BUILD_TYPE=RelWithDebInfo
cmake --build build
sudo cp build/bannertool /usr/local/bin/
```

> `git clone` 报 `Error in the HTTP2 framing layer` 的话:
> `git config --global http.version HTTP/1.1` 再试。

> **bannertool 只用来生成横幅**(主菜单选中时上屏那段动画)。
> 图标已改用普通构建产出的 `.smdh`,不再需要它。
> 装不上也不影响 `.3dsx` —— `make` 照常出包,只是没有 `make cia`。

**打包**:

```bash
make cia      # 产出 bilibili.cia
```

**安装到机器**:把 `bilibili.cia` 拷进 SD 卡,主机上打开 **FBI**
(装自制系统时一般都装了)→ SD → 选中 cia → Install。
卸载走"系统设置 → 数据管理",和正式软件一样。

工程里 `cia/` 目录的内容:

- `3danmu.rsf` — 打包配置。**CIA 想用 MVD 硬解,有四个条件缺一不可**,
  而且缺哪个的症状完全不同,单独排查很难对上号:

  | 条件 | 缺了会怎样 |
  |---|---|
  | `ServiceAccessControl: mvd:STD` | 连不上服务 |
  | `Dependency: mvd: 0x0004013020004102` | 模块不会被加载,IPC 无限挂起、整机死锁 |
  | `ReleaseKernelMinor: "44"`(>= 2.44) | init 假装成功,第一次送帧时 mvd 进程 svcBreak |
  | 传给 mvd 的线性地址转到 `0x30*` 窗口 | mvd 进程 data abort,FAR = 你的缓冲地址 |

  (3dsx 经 HBL 启动时继承的是宿主的全量权限和内核版本,所以这四条以前都不用管);
  同理 `http:C`、`soc:U`、`ssl:C`(网络)、`dsp::DSP`(音频)、
  `ndm:u` 都不能少。New3DS 加速三件套 `SystemModeExt: 124MB` /
  `CpuSpeed: 804MHz` / `EnableL2Cache: true` 也在这里声明
- `icon.png` 48x48 / `banner.png` 256x128 / `audio.wav` — 图标、上屏横幅、
  打开时的提示音(现在是自动生成的小电视图案,欢迎自己画一套换掉)
- 根目录的 `3danmu.png` 给 3dsx/HBL 用,与 CIA 图标同款

> UniqueId 用的 `0xB1B11`,如果和你机器上其他自制软件撞了(概率极低),
> 改一个重新打包即可。

---


---

## 发布流程

全量重编 + 打包一条命令:

```bash
./tools/package.sh
```

它会:检查 romfs 和 licenses 齐不齐、确认 ffmpeg 没被改成 GPL 配置、
`make clean` 后全量重编、打出**二进制包**和**源码包**两个 zip,
并把本次的 `.elf` 存进 `release/`。

三件事值得单独说:

- **必须 `make clean`。** devkitPro 的 Makefile 只跟踪源文件,
  改了 romfs 或 TARGET 名时增量构建会拿旧产物糊弄你(见「从源码编译」)。
- **两个 zip 必须一起发。** FFmpeg 是静态链接的 LGPL 库,
  只发二进制不发源码不合规(见 THIRD-PARTY-NOTICES.md 第 1 节)。
- **留住 `.elf`。** 用户发来 Luma 崩溃页时,`addr2line` 需要
  **与该版本完全一致**的 elf 才能定位到源码行。重编过就对不上了 ——
  这个项目里最快的一次破案(五分钟从崩溃页找到 `httpc_reset`)
  就是靠手边正好有那份 elf。

---

## 代码结构

```
3danmu/
├── Makefile                 devkitPro 标准 3DS 工程配置
├── build-ffmpeg-3ds.sh      ffmpeg 交叉编译脚本
├── README.md
├── romfs/                   打包进程序的资源(放 font.bcfnt)
├── test/
│   └── host_test.c          PC 端单元测试
└── source/
    ├── main.c               场景循环:列表 / 搜索 / 登录 / 设置
    ├── ui.c/h               citro2d 双屏渲染:文本、按钮、二维码、调试台切换
    ├── net.c/h              httpc 封装:GET/POST、重定向、cookie、Range 流
    ├── bili.c/h             B 站 API 层
    ├── wbi.c/h              请求签名(纯逻辑,已单测)
    ├── md5.c/h              MD5(纯逻辑,已单测)
    ├── jsonx.c/h            路径式 JSON 访问(纯逻辑,已单测)
    ├── danmaku.c/h          弹幕:异步加载线程 / 解压 / 解析 / 滚动渲染
    ├── player.c/h           播放器(三线程流水线)
    └── vendor/
        ├── jsmn.h           JSON 分词器(MIT)
        └── qrcodegen.c/h    二维码生成(MIT)
```

各模块职责:

- **net.c** — 所有 HTTP 通信的唯一出口。封装 3DS 的 `httpc` 服务,
  处理重定向、cookie 读写与持久化、以及播放用的 Range 流式下载
- **bili.c** — 把 B 站 API 翻译成 `BiliVideo` 结构体。包含签名调用、
  gzip 自动解压、错误信息回传 UI
- **player.c** — 最复杂的模块,约 1100 行,详见下节
- **ui.c** — 所有绘制的底层原语,双屏 render target 管理

---


---

## 技术架构详解

### 三线程流水线

```
 ┌──────────────┐   环形缓冲     ┌────────────────┐   帧邮箱    ┌─────────────┐
 │ 下载线程     │  1.5MB ring    │ 解码 worker    │  双缓冲     │ 主线程      │
 │ (core1)      │ ─────────────► │ (core2)        │ ──────────► │ (core0)     │
 │              │                │                │             │             │
 │ httpc 拉流   │                │ ffmpeg 解封装  │             │ 输入处理    │
 │ 断线重连     │                │ MVD/软解       │             │ GPU 合成    │
 │ Range 定位   │                │ AAC → NDSP     │             │ 弹幕/GUI    │
 └──────────────┘                └────────────────┘             └──────▲──────┘
                                                                       │
 ┌──────────────────────────────┐   加载完成后原子发布                  │
 │ 弹幕加载线程(core3,低优先) │ ─────────────────────────────────────┘
 │ 下载 → inflate → XML 解析    │   仅在开播初期存在,完成即退出
 └──────────────────────────────┘
```

播放期间常驻三个线程;点开视频时额外有一个短命的弹幕加载线程。

**为什么要三个线程**

- 单线程时,解码一卡音频就断;New 3DS 有 4 个核心,不用白不用
- 网络和解码分离后,网络抖动被环形缓冲吸收(约 20 秒余量),播放不受影响
- 主线程只做轻量工作,输入响应始终跟手

**线程间通信**(无锁,单生产者单消费者 + `volatile` + `__dmb()` 内存屏障)

- 网络 → 解码:环形缓冲,`rd`/`wr` 为单调递增的 u32 计数,取模得下标
- 解码 → 呈现:1 槽邮箱(`mb_full` / `mb_pts_ms` / `mb_buf`)+ 帧双缓冲。
  邮箱机制天然保证了"上一帧被消费前不会开始写下一帧"

### 播放时钟与音画同步

以**音频为主时钟**:`samples_done + ndspChnGetSamplePos(0)` 除以采样率,
精度到单个采样(约 30 微秒)。视频帧携带 pts,主线程比较 pts 与时钟决定何时呈现。

**时钟基准要用真实 pts 校准**:音频流的首个 pts 未必是 0(常有几百毫秒偏移),
若直接从 0 起算,弹幕(按绝对时间投放)就会整体偏移。因此开播和每次 seek 后
都置 `clock_resync`,由首个解码出的音频帧的 pts 重设时钟基准。

无音频时(缺 dspfirm.cdc)退化为系统时钟。

### 缓冲态(和手机 App 一样的"缓冲中")

数据吃干时:**冻结音频 → 冻结时钟 → 画面定格 → 显示"缓冲中…"**;
后台继续拉流,攒够约 1 秒音频 + 256KB 网络余粮才恢复。
迟滞设计避免"播半秒卡半秒"的抽搐循环。起播时也走一遍缓冲态。

进入缓冲的判据是"空闲 wavebuf ≥ NBUFS-2"(只剩约 90ms 音频),
**不是**"一个都不剩"。等彻底吃干才冻结的话,这中间 NDSP 已经在放静音,
而时钟还照常往前跑,恢复后音画就永久错开那一截——
音画不同步的累积量正是这样一次次攒出来的。提前一点冻结,
时钟和真正出声的样本始终对得上。

### 自适应跳帧(软解)

软解跟不上时,视频会越落越远。策略:落后超过阈值 → 开启
`skip_frame = AVDISCARD_NONREF`(跳过非参考帧)+ 关闭环路滤波;
追平后恢复完整解码。阈值由"同步/流畅"模式决定(150/50ms 或 400/150ms)。

### 视频呈现:GPU 纹理合成

解码帧(RGB565)→ `C3D_SyncDisplayTransfer` 由 PPF 引擎搬进平铺纹理
→ `C2D_DrawImageAt` 缩放绘制 → 上面叠弹幕、进度条、缓冲提示。

纹理尺寸取 **≥ 对齐后视频尺寸的 2 的幂**,宽高分别计算:

| 视频 | 纹理 | 显存 |
|---|---|---|
| 854x480(480P 横屏) | 1024x512 | 1MB |
| 640x360(360P 横屏) | 1024x512 | 1MB |
| 360x640(竖屏) | 512x1024 | 1MB |

早期版本纹理宽固定 1024,竖屏视频要 1024x1024 = 2MB,
加上两个同样行距的解码缓冲,linear 内存直接耗尽 → 播放失败。

画面方向由 `video_draw_top()` 里的 `subtex.top/bottom` 固定决定
(当前 top=1.0、bottom=1-vh/tex_h);若哪天出现上下颠倒,对调这两行即可。

#### 主线程预算:缓存维护是隐形杀手

上屏路径经过一轮专门优化,起因是"播放时偶尔顿一下 + 音画慢慢错开"。
根子在**主线程每帧干的活太多**,而 3DS 主线程还要跑 60Hz 的 GUI:

| 每帧动作 | 数据量(640x360) | 优化前 | 优化后 |
|---|---|---|---|
| MVD 输出 → vout 行距转换 memcpy | ~460KB | 主线程 | **worker(core2)** |
| 整帧 `InvalidateDataCache` | ~470KB | 主线程 | **worker,且每帧只 1 次** |
| vout `FlushDataCache` | ~750KB | 主线程 | **worker,且只刷写过的行(~460KB)** |
| `C3D_SyncDisplayTransfer` | — | 主线程 | 主线程(只剩这一件) |

关键认识:`GSPGPU_Flush/InvalidateDataCache` 落到
`svcFlush/InvalidateProcessDataCache`,是**内核调用**,执行期间持内存管理锁,
会连带把其它核上的线程一起停住。它不是"顺手做一下"的操作,
几百 KB 一次、一秒几十次,足够把 GSP 挤到掉帧。

因此还有第二处优化:**角标探测只维护 4 条 cache line**。
MVD 有没有出帧靠四角的 0x11 标记判断,只关心 4 个字节,
但旧代码每次探测都 Flush/Invalidate 整个输出缓冲——而排空循环
(`mvdstdRenderVideoFrame` 遇 BUSY 重试)每轮都要探测一次,
单帧几百 KB 起步。现在按 32 字节 cache line 逐个维护,
整帧失效只在真要读像素(搬运)前做一次。

同时给排空循环加了让步:`BUSY` 连续 8 次后每轮 `svcSleepThread(0.1ms)`,
不再死等——worker 和网络/音频线程共享核心,空转会把它们饿住。

#### 文字渲染:一个病根,两种症状

实测报告过两个看似无关的问题:**「主界面上下滚动掉帧」** 和
**「弹幕一多画面就变形」**。查下来是同一个根源——`ui.c` 的文字测量。

问题一,`ui_text_clipped()` 的截断算法是 **O(n) 次完整字形解析**:
每次砍掉一个字符,重新量一遍整串宽度。一个 40 字的标题要量 20 多遍。
列表可见 4 行 × (标题 + 元信息) × 60fps = 每秒几十万次字形查表。

问题二更隐蔽:**测量和绘制共用同一个 `C2D_TextBuf`**,
而该缓冲每帧只在 `ui_begin()` 清一次。测量吃掉的字形槽不会归还,
弹幕多的时候(单帧上限 80 条,3D 模式双眼各画一遍)槽位被吃光,
后面 `C2D_TextFontParse()` 就返回 NULL——而旧代码**不检查返回值**,
栈上那个未初始化的 `C2D_Text` 被原样交给 `C2D_DrawText`,
它拿着随机的 begin/end 下标和随机的 sheet 指针去画,
屏幕上就出现莫名其妙的方块和拉伸色带。所谓「画面变形」不是显存不够,
是**渲染读了垃圾内存**。

三处修改:

1. **测量改用独立的小缓冲**(512 字形)并即时清空——测量再也不占绘制预算
2. **所有 `C2D_TextFontParse` 检查返回值**,失败就不画这行字。
   缓冲真满了也只是少几条弹幕,不会再把渲染搞坏
3. **截断改二分查找**(约 8 次测量),**并按内容缓存结果**——
   同一串在后续帧直接命中,一次测量都不做

同理,字幕折行(逐字加宽找断点)也改成算一次缓存在 `SubLine.cut` 里;
列表跑马灯的标题宽度也只在换行时重算。

**缓存容量要盖得住工作集**:第一版截断缓存只开了 24 条,
而一页 20 条视频 = 40 个串(标题 + 元信息)。滚动时新行进场不断把旧行挤出,
往回滚又要重算——症状从「一直卡」变成了「偶尔卡一小下」。
容量提到 64 之后才真正稳定。**缓存开小了不会报错,只会偶尔漏一拍**,
这类问题得按工作集大小算,不能凭感觉取整。

> 教训:在 3DS 上,`C2D_TextFontParse` 这种「看起来只是解析字符串」的调用
> 其实是每帧成本最高的操作之一,而且**失败是静默的**。
> 凡是返回指针的 citro2d 接口都要检查——不检查的话,
> 症状会以完全不相干的形式出现(这次是「画面变形」)。

#### 内存占用

顺手清掉了几处纯浪费(以 640x360 横屏为例):

| 项 | 之前 | 之后 | 省 |
|---|---|---|---|
| `vout` 双缓冲 | 按纹理高 tex_h 分配 | 按实际用到的 vh_al 行 | **~590KB** |
| `mvd_raw` | 双缓冲 | 单缓冲(搬运已同步) | **~470KB** |
| `VQ_CAP` 视频包队列 | 96(约 3 秒) | 48(约 1.5 秒) | ~0.5MB 堆 |
| 绘制字形缓冲 | 8192 | 6144 | ~80KB |

`vout` 那一项是典型的「多分配了没人读的部分」:上传时是
`GX_BUFFER_DIM(tex_w, vh_al)`,纹理高度以下从来没被读过,却按 tex_h 分配。

`mvd_raw` 的双缓冲则是重构后的遗留:搬运挪进 worker、且在角标确认后
**同步**完成,MVD 不可能在搬运期间回写,第二面就没有存在意义了。

抗网络抖动本来就是 1.5MB 环形缓冲的职责,视频包队列只需要盖住
「解复用领先解码」的那点抖动,96 深纯属冗余;内存宽裕了,
`av_packet_clone` 因内存不足失败导致的丢包也跟着少了。

#### 追帧:硬解也要能丢帧

软解早就有自适应跳帧(见下),**硬解原本一点追赶机制都没有**:
邮箱里的帧无论多旧都照样上屏,一旦解码/网络短暂跟不上,
视频就永久落后音频一截,而且只会越积越多。

现在主线程呈现时判断:该帧比音频时钟迟到 **>400ms** 就直接丢弃,
不上传纹理,只清空邮箱让 worker 立刻去解下一帧,画面很快追回。
连续丢弃上限 2 帧——再怎么落后也不能让画面整个停住。

**但追帧必须能自己认输。** 这招只在「落后是暂时的」时候管用:
如果落后是结构性的(音视频时间戳基准本来就差一截、pts 队列记错),
丢多少帧都追不上,只会变成「丢 N 张放 1 张」的循环,
画面掉到几帧每秒**而声音完全正常**——听起来一切正常,看起来一直在卡。
所以每轮丢完都回头对比落后量有没有真的减小,连续三轮无效就
**永久关掉追帧**,老老实实按序播。

> 通用教训:任何「丢弃工作以求追上」的机制都必须带效果检验。
> 追不上还继续丢,比不丢更糟——而且症状会伪装成别的问题。

#### 先量再改:pts 队列漂移

「视频稳定落后音频 442ms、丢帧追不回来、吞吐看着又没问题」——
这个组合我连猜错两次(先怀疑单缓冲,再怀疑主机时钟),最后老实上秒表:

```
prof/frame: mvd=6.5 inval=0.3 copy=2.0 fl=0.6 ms
```

合计 9.4ms,而 30fps 的预算是 33ms —— **3.5 倍余量,解码路径完全不是瓶颈**。
顺带也把「内核缓存调用很贵」这个先验推翻了:整帧失效 0.3ms、刷新 0.6ms。

那 442ms 只能来自**时间戳标签**。pts 队列的约定是「每送一个包压一条 dts,
每出一帧弹一条」,理想情况下队首正是本帧的时间戳。但
**「送了包却没出帧」**(起播预热、seek 后 MVD 重开、丢包)会留下多余条目,
此后每一帧都被贴上早了 N 帧的标签,而且这个偏差永远不会自己消失。

后果比单纯的音画不同步更糟:标签总是「早就该显示了」,主线程于是
一解出来就立刻上屏,**画面彻底失去按时钟出图的节奏**,
变成「有多少包放多快」——帧间隔不均匀,看着就是时不时顿一下。
这正是「吞吐只用掉 9/33ms 却照样卡」的答案。

两处修改:队首明显落后音频时钟(>250ms)就多弹几条把标签追上来
(`pts drift: trimmed N`);在途深度上限从 8 压到 4,把最坏偏差
从 266ms 限制到 133ms。

> 教训:这一节的每一条结论都是量出来的,而前面两次猜测都很"合理"——
> 单缓冲的竞态、3DS 时钟不准,听起来都像那么回事。
> **性能问题上,合理的假设和正确的假设没有关系。** 秒表应该是第一步,
> 不是第三步。

#### 保护机制自己制造了它要防的故障

上一条改完出现新症状:偶尔花屏,并伴随 `mvd rejected pkt x3, dropping`。
这是**我加的保护机制在误伤**。

`MVD_CONSUMED` 只在真正送包时才置。而 `mvd_skip` 为真时,这次调用是去
**排空 MVD 内部已经解好的帧** —— 出了帧、但确实没吃这个包,
这是完全正常的流水线行为。第一版把这种情况也计入「重试」,
于是 MVD 内部一旦排着 3 帧以上,连排三次就把一个好端端的包丢了。
丢包断参考链 → 花屏。为了防丢帧加的机制,自己制造了丢帧。

判据错在把「有没有吃包」当成了「有没有进展」。正确的判据是:
**出了帧就是有进展**,计数清零;只有既没出帧又没吃包才算原地打转
(而这几乎不会发生,因为 attempt 循环第二轮必定送包)。

> 教训:给循环加保护时,「进展」的定义必须覆盖**所有**表示进展的形式。
> 漏掉一种,保护就会在正常路径上开火 —— 而且伪装成它本要防的那个故障,
> 让人以为原来的问题更严重了。

#### 无限重投:MVD 不买账就原地打转(诊断有误,见上)

`stall 950ms / ring=1536KB / vq=48 / mb=0` —— 数据管够、包管够、没有帧。
这个特征出现过两次,第一次误判成单缓冲的竞态(回滚后又复现,说明猜错了)。

真正的机制在 worker 的重投逻辑:`mvd_decode_packet` 若没吃下这个包
(没置 `MVD_CONSUMED`),就把它退回队首下轮重投——而且**没有次数上限**。
MVD 一旦对某个包不买账(码流损坏、参考帧缺失),worker 就在这一个包上
原地打转,每轮还要空烧两次排空轮询,画面整秒不动而音频照放。

现在重投 3 次仍不吃就丢掉。掉一帧最多花屏一瞬,参考链会在下个 IDR 自愈,
比冻结一秒好得多。同时排空上限压到约 10ms(单次最坏),
worst case 每包 4 次 x 10ms 有界。

> 教训:**任何「失败就重试」的路径都必须有次数上限**,否则一个坏输入
> 就能把整条流水线钉死。这个 bug 和前面「追帧不检验效果」是同一类
> ——循环缺少退出条件。

关于探针阈值也有一课:`decoder no frame` 原本设的是**连续 40 个包**,
而实测那次卡 950ms 也就重投二十几轮,**刚好够不到报警线**。
探针的阈值高过了要抓的现象,等于没有探针。现在是 8。

#### 真凶:渲染循环里发了个 HTTPS POST

这个卡顿查了整整五轮,前四轮全在查解码器,方向从头到尾是错的。
决定性的线索是一句话:**「怎么弹幕也会同时停」**。

弹幕**根本不经过解码器** —— 它由主线程按真实经过时间推进
(`dm_clock += dt`),跟 MVD、跟视频帧一点关系都没有。
画面和弹幕同时停,就只有一个可能:**主线程自己被卡住了**。
而音频照放,因为 NDSP 由 DSP 自己喂,主线程停了也不影响。

真凶是观看进度上报:`bili_report_history()` 是一次 HTTPS POST,
在 3DS 上要几百毫秒到一秒多,而它**直接写在渲染循环里**,每 15 秒一次。
每次上报,整个主线程停摆一次 —— 这就是「偶尔卡一小下」,
以及为什么它的周期性那么难抓(15 秒一次,而且只在登录后才有)。

现在主线程只置一个请求标志,POST 交给一条专门的 reporter 线程。
顺带把字幕加载也堵上:`sub_load_async()` 会被渲染循环调用,
而它内部的 `sub_free()` 带一个**超时 10 秒**的 `threadJoin` ——
上一轮线程若还卡在 HTTP 上,主线程就会陪着一起等。改成在途直接跳过,
下一帧再问。

还加了主线程看门狗:一轮循环超过 120ms 就打 `main loop blocked Nms`。
主线程里能阻塞的地方不止一处(网络、threadJoin、GPU 同步传输),
与其一个个猜,不如让它自己报出来。

> 这一节最该记住的不是结论,而是**那条线索为什么有用**:
> 弹幕和画面走的是两条完全独立的路径,它们同时停,
> 立刻就把「所有只影响视频路径的解释」全部排除了 ——
> 包括我前面查的单缓冲竞态、pts 漂移、MVD 拒收。
> 排查性能问题时,**找一个不该受影响的东西看它有没有受影响**,
> 比继续深挖嫌疑最大的那条路径有效得多。

#### 探针要能自己解释自己,而且不能刷屏

调试这轮卡顿时,`prof` 一度只打 `call=150 frm=110` —— 拿到这个数
**谁也判断不了正不正常**:送一个包不一定立刻出帧(H.264 帧重排),
排空阶段又反过来「出帧但没送包」,调用数天然多于帧数。
补上窗口时长和片源帧率之后才有意义:

```
prof: 3670ms fps=30.2/30 call=150 frm=110 nofrm=40 mvd=7.1 inv=0.3 cp=2.0 fl=0.6
```

`fps=30.2/30` 前者实测、后者片源,两个数贴着就是健康的,
`call/frm` 的比例根本不用看。(略高于片源是正常的:片源多为 29.97,
且队列满时「解了就扔」的帧也计入解码计数。)

> **指标必须能自己解释自己。** 需要旁边有人告诉你「多少算正常」的数字,
> 不是指标,是噪音。

另一条同样重要:**常态刷屏的探针,等于把别的探针都关了**。
日志环形缓冲只有 160 行,`prof` 每 5 秒一条,十几分钟就能把真正有用的
历史(取流失败原因、字幕轨、卡顿现场)全冲干净。所以现在
`prof` 只在头两个窗口和确实异常时打(帧率掉到片源 85% 以下,
或单次调用均摊超 20ms),`vq full` 之类高频路径也都做了递增节流。

#### 卡顿探针

「画面卡一下但声音不卡」这种现象,肉眼分不出是哪一段断供。
播放器里常驻一个探针:画面停超过 250ms(且不在暂停/缓冲/跳转)时,
往调试台打一行现场快照,每 2 秒最多一条:

```
stall 310ms ring=48KB vq=0 mb=0 late=120
```

| 现场 | 结论 |
|---|---|
| `ring` 很小 | 网络没跟上,下载线程饿死了解复用 |
| `ring` 大但 `vq=0` | 解复用/解码跟不上,worker 卡住了 |
| `vq` 有货但 `mb=0` | 解码器没出帧(MVD 排空、参考帧缺失) |
| `mb=1` 却不上屏 | 时间戳问题(帧没到点,或追帧正在丢) |

片尾必须排除:最后一帧停在屏上等剩余音频放完,现场同样是
`ring=0 vq=0 mb=0`,和「真卡住」长得一模一样(`p->dbg_eof` 就是为此加的)。

解码侧还有一个配套探针:连续 40 个包解不出帧就打
`decoder no frame xN (consumed=? vq=?)`。`consumed` 能进一步区分
「MVD 拒收这个包」和「收了但不吐帧」。

#### 单缓冲 mvd_raw:一次失败的省内存尝试(已回滚)

把 `mvd_raw` 从双缓冲改成单缓冲省了 470KB,理由看起来很硬:
搬运已经挪进 worker 且是同步完成的,MVD 不可能在搬运期间回写。
**真机直接翻车**——起播后解码器一帧都不出,探针现场是
`ring=1536KB vq=48 mb=0`(数据管够、包管够、就是不出帧)。

原因在角标机制本身:下一帧的 0x11 角标要写在**MVD 可能仍在收尾的
那块内存**上,标记和 MVD 的写回互相踩,出帧判定就此失灵。
双缓冲的价值不只是「两块内存轮流用」,而是保证
**正在标记的那面没有任何人在碰**。已回滚。

> 教训:双缓冲常常不是为了性能,而是为了给「谁在写这块内存」
> 这个问题一个确定的答案。省内存前先问清楚它当初是为谁而设。

### 跳转(seek)

主线程拖动进度条 → 松手时置 `seek_req` → worker 线程处理:
`av_seek_frame(AVSEEK_FLAG_BACKWARD)` 定位到目标前最近的关键帧 →
清空视频包队列 → `avcodec_flush_buffers` 冲刷解码器 + `swr_init` 复位重采样器
→ **MVD 排空内部帧** → **`ndspChnReset` 彻底重置音频通道** →
标记时钟待校准 → 重新进入缓冲态。

这条路上踩过的坑(全部实测复现并修复,按当时的排查顺序):

1. **必须 `ndspChnReset`,不能只 `ndspChnWaveBufClear`**。音频时钟 =
   `samples_done + ndspChnGetSamplePos(0)`,只清前者的话通道内部的采样位置
   计数会留着上一段的残值,于是**每跳转一次就多加一次偏移,拖得越多越不同步**。
   Reset 之后要重新 `SetInterp` / `SetRate` / `SetFormat`,并让暂停态重新生效。
2. **时钟要用真实 pts 校准,不能用"拖到的位置"**。`av_seek_frame` 落在目标
   之前最近的关键帧,可能早好几秒;因此 seek 后置 `clock_resync` 标志,
   在首个解码出的音频帧到达时用它的 pts 重设时钟。
3. **环形队列满时不能"塞回队首"**。原实现在包未被解码器消费时会把它退回
   队首等待重投,但队列已满时**队首与队尾是同一个槽位**,紧接着写入的新包
   会覆盖它 → 指针丢失 + `vq_len` 越界成 `CAP+1` → 后续同一指针被释放两次
   → 第二次跳转时崩在野指针上(`data abort / Permission - Read`,FAR 是垃圾值)。
   现在队列满时直接丢弃,退回操作也加了容量检查。
4. **seek 后的 MVD 只能关掉重开,不能"排空"**。曾实现过一个 `mvd_flush`:
   反复 `RenderVideoFrame` 把内部残帧倒出来丢掉——结果把 **MVD 系统模块
   本身搞崩了**(Luma 异常界面显示 `Current process: mvd`,`svcBreak`,
   整机死机)。这种自创的排空姿势超出了 MVD 状态机的预期。正解是 seek 时
   `mvdstdExit()` + `mvdstdInit()` 整个重开:这是每次播放都在走的路径,
   已验证稳定;重开后重喂 SPS/PPS(启动时存了副本)、首包重复送一次。
   代价只是 seek 多几十毫秒,反正之后本来就要重新缓冲。重开失败则整体
   降级软解(-99 路径)。
5. **seek 前的残帧会在新画面前闪一下**。worker 在 seek 处理里清了邮箱
   (`mb_full=0`),但主线程可能**已经**读到 `mb_full=1`、正准备上传那帧——
   竞态窗口里旧帧照样上屏。修法:给每次 seek 递增一个代数 `seek_gen`,
   worker 发布帧时盖上当前代数,主线程发现帧的代数对不上就直接作废。
这是"拖动次数越多越不同步、
   再拖就黑屏"的元凶。MVD 的时间戳不是它给的,是我们自己维护的一个队列:
   每送一个包入队一个 dts,每吐一帧出队一个。seek 时把队列清零、却没清
   解码器里排着的三五帧旧画面——它们随后照样吐出来,每吐一帧就吃掉一个
   属于新位置的时间戳,于是**此后每帧的时间戳都错位一格,而且每 seek
   一次错位量再涨一截**,表现就是越拖越不同步。错位攒到队列被填满后,
   帧会拿到远在未来的时间戳,呈现逻辑"等它到点"就永远等不到 → 黑屏定格。
   时间戳侧仍保留两道防线:队列设在途上限(8),满了丢最老的;帧时间戳与
   音频时钟差超 3 秒判为不可信,清队列改用等间隔时钟,呈现侧对"远未来"
   时间戳直接上屏不死等。
5. **暂停中也要处理 seek**。worker 循环里暂停分支原本在 seek 处理之前,
   暂停时直接 continue——拖动松手后请求一直挂着,进度条弹回旧时钟,
   恢复播放才突然跳到新位置。修法:暂停时只要有 `seek_req`(或 seek 刚
   完成还没解出画面)就照常走一轮循环;seek 处理器在暂停状态下会置
   `need_frame`,让解码继续跑到产出第一帧为止——暂停画面立刻变成新位置
   的画面,进度条也停在新位置。片尾解不出帧时清掉标志防空转。
6. **重采样器也要复位**。`swr` 内部压着上一位置的采样,不清的话它们会被拼在
   新位置音频的前面,每 seek 一次音频就整体后移一点点——又是一个随拖动次数
   累积的偏移。`swr_init()` 可重复调用,作用就是复位内部状态。
7. **松手后进度条闪回旧位置——两层原因**。第一层是显示:`seek_req` 还挂着
   的几帧里 UI 拿到的还是旧时钟,改为跳转期间显示 `seek_to` 即可。但修完
   仍会闪,挖出第二层:**时钟校准会真的回跳**——`av_seek` 落在目标前最近
   的关键帧(可能早好几秒),首个音频帧到达时 `clock_resync` 把时钟校准到
   关键帧 pts,进度条如实回跳再追回来。根治 = **精确 seek**(官方 App 同款):
   关键帧→目标之间照常解码维持参考链,但音频帧、视频帧全部丢弃不出声不上屏
   (丢弃路径注意自己释放 AVPacket,`continue` 会跳过循环尾部的统一释放),
   时钟钉在目标位置。副作用是拖动落点也变准了——以前会落到关键帧,
   现在正好落在松手的位置。此外 UI 侧还有一层**目标锁存**:松手后进度条
   锁在目标位置,直到时钟落到目标 **±0.3 秒内**(或超时 4 秒)才交还。
   注意解除条件必须是"落在附近",不能写成 `clock >= 目标`——向前跳(倒回)
   时旧时钟本来就大于目标,`>=` 会让锁存当帧失效,表现为"向后跳好了、
   向前跳还闪"。另有侦测器:非跳转期时钟倒退超 0.5 秒打 `clock regressed`
   日志。`av_seek_frame` 失败(向前跳要重新发起 HTTP Range,偶发超时)
   也不再静默吞掉:打日志并重试 3 次。
8. **`av_packet_clone` 失败会让 NULL 进队**。3DS 内存紧,反复 seek 造成
   的碎片会让 clone 偶发失败,返回的 NULL 原样入队,弹出解码就是空指针
   崩溃(又一个"多拖几次就崩")。修法:clone 失败丢包并打日志,绝不入队;
   两处弹出点也各加一层 NULL 防线。
9. seek 时还要 `av_bsf_flush`(annex-b 过滤器)、把队列槽位置空、
   MVD 重新标记首包(重建参考链);主线程在上一次 seek 未完成时不接受新请求。

底层的 `avio_seek_cb` 会先判断目标是否落在环形缓冲已有数据内:
是则直接移动读指针(零网络开销,小幅跳转很快);
否则请求下载线程用 HTTP Range 重新定位。

### 弹幕

**异步加载**(为什么这样设计):弹幕文件几百 KB,下载 + inflate + 解析
串行执行会让开播多等一两秒。但这段时间播放器本来就在等网络(连接、探测、
缓冲),CPU 是闲的——所以把弹幕丢到独立线程与之并行,等待时间直接被吸收掉。

- 线程落在 **core3**(New 3DS 的第二个附加核心,平时完全空闲),
  建不了就退到 core1,再不行就同步加载兜底;优先级 0x38(低于解码与呈现),
  不会跟播放抢 CPU
- **行数随字号铺满全屏**:行数 = 234/行高(小 17 行、中 14、大 11),
  不是写死的——否则小字号只占上半屏。改字号时行号全部重排
- **音频缓冲满时绝不能继续解码**。`audio_feed` 原来无条件
  `receive_frame` + `swr_convert`,而 pending 满时 `swr_convert(space=0)`
  会把这一帧**直接丢掉**——音频缺一段、视频不缺,就是永久性音画不同步。
  长时间缓冲(等弹幕、网络卡)最容易触发。修法:没空间就不取帧,
  让帧留在解码器内部排队;`want_read` 也加上音频空间检查
  (原条件里 `vq_len < VQ_CAP/2` 那一支会绕过检查继续读)
- **弹幕不能抖**:弹幕位置 = `(clock - t) / 8s` 换算出的横坐标,所以时钟只要
  抖一下,整屏弹幕就会一起"错格"。起播头几秒尤其明显,两个源头都修了:
  1. **音频时钟单调化**(worker):一个 wavebuf 播完到被 `audio_reap` 回收
     之间有个窗口——`ndspChnGetSamplePos` 已经归零、`samples_done` 还没加上
     去,时钟会瞬间倒退最多 62ms。现在小幅倒退(<300ms)一律按住不放,
     只有 seek/重置这种大跳才接受
  2. **主线程平滑时钟**:`clock_ms` 由 worker 每轮循环发布一次,而起播时
     worker 正忙着初始化解码器、猛灌缓冲,发布间隔忽长忽短;渲染却是 60fps。
     直接拿来用就是一顿一顿的。现在主线程自己按 `osGetTime` 匀速推进,每帧
     再向权威时钟收敛 8%(时间常数约 0.2s),暂停/缓冲时不推进,seek 或偏差
     过大时硬对齐。既跟得住音频,又不会把时钟的台阶原样画出来
- **字幕选轨与"串台"**,连踩三次:
  ① 最初写成 "lan 以 zh 开头 **或** 还没选到就用它",后半句把英/日轨也抓
  进来,表现为"没有中文字幕的视频也能开出字幕,而且是别的语言"→ 改为只认中文;
  ② 换视频时旧线程 join 超时后仍会把上一轮结果写回来 → 加代际号作废;
  ③ 最顽固的一次:**第一个视频就串台,退出等一会儿再进就正常**。
  最后这句话是破案关键——它排除了"id 传错"(id 错的话第二次也该错),
  指向**并发时序**:`thumb_stop()` 原本放在整条请求链之后,于是
  取cid / 字幕 / 弹幕 / 取流 会与**两条仍在跑的封面下载线程**五路并发,
  而封面用的 `net_get_img` 当初为了并行**故意绕过了 net 串行锁**。
  3DS 的 httpc 在这种多线程压力下会把响应张冠李戴,字幕线程收到的
  就是别的请求的响应体。等一会儿再进正常,是因为封面已下完、并发消失。
  几轮加固后仍复现,最终**把字幕请求整个挪到播放开始之后**(缓冲结束、
  弹幕下完、再等 1 秒才发),从时间上彻底避开并发窗口;同时
  进播放第一件事就 `thumb_stop()`、图片请求纳入同一把锁、
  **keep-alive 分场景**(API 关、图片开)、字幕请求改用 `bvid`+`cid`。
  注意 keep-alive 不能一刀切关掉:封面是同一主机连续 20 张,每张重新
  TLS 握手会把系统核吃满,**列表滚动明显掉帧**——图片开、API 关才是平衡点。
  代价是字幕比画面晚几秒出现,换来的是不会串台
- **换视频要用代际作废旧结果**。字幕/弹幕都是后台线程加载,`sub_free()`
  join 有 10 秒超时——超时后旧线程仍在跑,回来时会把**上一个视频的字幕**
  写进全局变量,于是"开字幕出现的是刚才看的那个视频的字幕"。修法:
  每次 `sub_load_async`/`sub_free` 递增代际号,线程启动时捕获,
  发布前比对,不符就丢弃结果。另外"没有 aid/cid 所以跳过加载"的分支
  也必须调 `sub_free()` 清干净,否则同样残留
- **时间基准**:弹幕时间戳是相对视频内容起点的绝对秒数,播放时钟来自音频
  采样计数,并在首个音频帧到达时按其真实 pts 校准(音频流的首个 pts 未必是
  0,不校准弹幕会整体偏移几百毫秒)。调试台起播时会打印
  `tbase: fmt=?ms audio=?ms video=?ms` 和 `clock sync to ?ms`,
  怀疑弹幕整体偏了就先看这两行
- **一条都不能跳过**,这是这块的第一原则。绕了两圈才定下来:
  - 先试过"弹幕等视频先播",结果**开头几秒的弹幕被永久跳过**(视频已经播过去了)
  - 又试过"数据晚到时只补最近 2 秒、更早的丢掉"来治糊屏,同样是漏弹幕
  - 最终:**谁都不跳过,改成让它别迟到**——
    ① `dm_load_async` 放在 `bili_get_play_url` **之前**,选中视频按 A 的那一刻
    就起跑(取流那几个 API 往返是等延迟、不吃带宽的,白送给弹幕);
    ② 首次起播时若还没就绪,就保持缓冲态多等一会(上限 20 秒,提示变成
    "载入弹幕…"),反正视频开头本来也要缓冲,两件事并行;
    ③ **加载失败重试 3 次**,退避 0.3 秒——播放同时发 httpc 请求偶尔会直接
    失败,以前失败一次整场就没弹幕了,多半的"弹幕加载不出来"是这个。
    seek 之后不再等待(弹幕早已在内存里)
- **无锁发布**:加载线程先写数组指针,`__dmb()` 内存屏障后再公布条数;
  渲染线程只读这两个变量,发布前看到条数为 0 自然什么都不画,不需要互斥锁
- **发弹幕**:播放页按钮 → 自动暂停 → 未登录先拉起扫码(登录回调由 main
  注入,避免 player 反向依赖场景代码)→ 拼音输入法输入 → `dm/post` 发送。
  本地即时显示(高亮色 + 描边),**显示时间前挪 1.2s**——挂在"此刻"的话
  弹幕起点在屏幕右缘之外,暂停中时钟不走,永远看不见;发送成败都显示,
  失败只记调试台;
  aid 在列表解析时顺带收集(推荐/收藏的 `id` 字段就是 aid),
  查 cid 时还会兜底回填
- 加载期间上屏右上角显示"弹幕加载中";退出播放时 `dm_free()` 会 join
  该线程(10 秒超时保护)
- 踩坑:`dm_load()` 内部原本调用 `dm_free()` 清理旧数据,而 `dm_free()`
  会 join 加载线程——在加载线程里执行就是**自己等自己,必然死锁**。
  拆成 `dm_clear_items()`(只清数据)和 `dm_free()`(数据 + 线程)后解决。

`dm/list.so` 接口返回 deflate 压缩的 XML(每条形如
`<d p="时间,模式,字号,颜色,...">文本</d>`)。流程:
拉取 → zlib inflate → 手写解析器提取时间/颜色/文本 → 按时间排序 →
超过容量上限则等间隔抽样 → 播放时按 `(clock - t) / 8秒` 计算横向位置滚动。

只渲染屏内的弹幕,文本宽度与行号都是懒计算并缓存。

**行号:入场时挑最空的一行**

原来在解析时就按 `i % 11` 硬轮排,不看忙闲轮到谁是谁,于是同一时刻涌进来的
一批弹幕必然首尾相叠。现在改成入场那一刻(`row == 0xFF` 时)才决定:
扫一遍 11 行,算出各行**上一条弹幕此刻的右边缘**,挑最靠左的那行。

```
行0  [已走了一半的弹幕]                 右边缘 x=180  ← 空
行1               [刚进场的弹幕-------]  右边缘 x=390  ← 挤
行2  (空行)                             视为 x=-∞     ← 最优先
```

右边缘越靠左这行越空,新弹幕跟在后面越不会追尾;空行永远最优先。
每行只需记住"最后一条的 `t` 和宽度"两个数,右边缘按当前 clock 现算。
开销是每条弹幕一次 11 次比较,且只在入场那一帧做。

**挑行不会丢弃任何一条**——最坏情况(同一秒涌进十几条、11 行全满)也是
挑相对最空的那行放上去,视觉上叠一点,但一定显示。真正会丢的是下面的容量上限。

**容量上限**(这些才会真的丢弹幕,所以按机型分开给):

| | New 3DS | 老 3DS |
|---|---|---|
| 内存条数上限(超出等间隔抽样) | 3000 | 1500 |
| 单帧最多绘制条数 | 80 | 48 |
| 解压缓冲上限 | 10MB | 6MB |

一条 `DmItem` 约 112 字节,3000 条约 330KB,HBL 下完全吃得下;
单帧 80 条文本绘制是 GPU 侧开销,老机型软解本来就紧,所以维持 48。
机型用 `APT_CheckNew3DS` 查一次并缓存。调试台会打印
`danmaku: N total, sampled 1/M`,能直接看出这个视频有没有被抽样掉。

### 一类值得单独说的 bug:随操作次数累积的偏移

`拖动越多越不同步` 这种现象出现过三次,根因每次都不同,但形状完全一样:
**某个状态在一次操作里只被清理了一半,残留量每次叠加一点**。

| 残留在哪 | 症状 | 修法 |
|---|---|---|
| NDSP 通道的采样位置计数 | 每跳转一次音频时钟多偏一段 | `ndspChnReset` 而非只 `WaveBufClear` |
| MVD 内部排队的已解码帧 | 时间戳错位一格,逐次加深,最后黑屏 | seek 时 MVD 关掉重开 |
| swr 重采样器内部缓存 | 每跳转一次音频整体后移一点 | seek 时 `swr_init()` 复位 |

经验:凡是"越用越糟"而不是"一开始就错"的问题,基本都可以直接去找
**seek/reset 路径上被漏掉的那个状态**,而不是去调阈值或加补偿量。
另外三者都不会单独暴露——修好一个,剩下的才显出来,所以要一层层剥。

### 裸眼 3D(左右分屏视频立体化)

3DS 是少数自带裸眼 3D 屏的设备,而 SBS(左右分屏)3D 视频每帧就是
"左半给左眼、右半给右眼"——两者天然适配,实现只花了几十行:

- 上屏开立体模式(`gfxSet3D(true)`)后,左右眼各有一个 400x240 的
  render target(`C2D_CreateScreenTarget(GFX_TOP, GFX_LEFT/RIGHT)`)。
  3D 开启时上屏画两遍,`video_draw_top` 把视频纹理的 **u 坐标切半**:
  左眼取 [0, 0.5)、右眼取 [0.5, 1),其余照旧
- B 站 3D 片源多为**半宽 SBS**(每眼横向压扁一半),半宽画面拉伸回
  全宽正好复原比例,和 3DS 每眼 400px 意外登对
- **3D 滑块 = 连续的会聚旋钮**。先分清两件事:**立体强弱由片源视差决定**
  (烘焙死的,软件无法缩放——游戏能是因为游戏实时改摄像机间距重渲染,
  视频没有深度图做不了重投影);滑块能调的是**会聚平移**——视频左眼 -px、
  右眼 +px,把整个深度范围往屏幕"里"推,推满 = 每眼 5px。低位轻推 ≈
  零平移,深度全由片源说了算;往上推场景逐渐沉进"窗户"里,自己找舒适点,
  手感对齐 3DS 游戏。弹幕反方向(交叉视差,x 3px)始终浮在视频前一层
- **重影的账**:平移与片源视差叠加过头会超出融像舒适区;滑块高位还有
  光栅屏固有串扰(高对比画面必现,官方游戏推满也一样)。两者都随滑块
  回拉缓解——重影不是故障,是深度的价格,滑块就是找平衡的旋钮。
  若低位也重影,嫌疑在片源(左右画面质量不一致)或弹幕层,关弹幕对照
- 视频是否为 SBS 没有可靠元数据,所以做成播放页手动开关(`3D:开/关`),
  搜"3D 左右格式"找片源;普通视频开 3D 会看到左右半屏各占一眼,关掉即可。
  **每个视频默认 2D**,要 3D 手动开
- **清晰度直接决定 3D 观感**:SBS 格式每眼只有源视频一半的横向像素,
  360P(640 宽)每眼只剩 320px,拉到 400px 必然发糊;**480P(854 宽)
  每眼 427px,几乎正好对上 3DS 每眼 400px 的物理分辨率**,清晰度天壤之别。
  3D 开启且当前为 360P 时,播放页会提示"建议切 480P"。
  管线本身无损:软解 sws 与 MVD 都是全分辨率进纹理,分眼只是切纹理坐标,
  没有任何一步先缩小再切半
- 2DS 全系没有 3D 屏,开关无效果但无害;离开播放页自动回 2D

---


---

## 列表封面与滚动

列表页从纯文字升级为**图文卡片**(封面 96x60 + 两行标题 + 数据行),
左摇杆**像素级平滑滚动**,十字键改选择时选中项自动滚进可视区,手感对齐
官方 App。实现要点:

- **封面加载全异步 + 双线程 + 走 HTTP**(thumb.c):3DS 上一次 TLS 握手
  要几百毫秒,20 张串行光握手就十几秒——所以图床请求**改走 HTTP**
  (公开图片,明文无妨,失败自动回落 HTTPS),再开**两条 loader 线程**
  原子分发任务并行拉取(等待型负载,两路重叠近乎减半),且图片请求走
  独立通道(`net_get_img`)不进全局串行锁。
  **loader 线程绝不放 core1**:GSP/HID 系统服务在 core1 上跑,解码挤过去
  主线程必卡(实测滚动顿挫),New3DS 用空闲的 core2/3、老机型回落主核
  低优先级;解码间隙 sleep 4ms 让出内存带宽;**滚动进行中上传预算为 0**
  (同步 GPU 传输 1-2ms 在滚动中肉眼可见),滚完再补。低优先级 loader 线程逐张
  `net_get`(B 站图床自带缩放参数 `@96w_60h_1c.jpg`,每张 5-15KB)→
  **ffmpeg mjpeg 解码**(build-ffmpeg-3ds.sh 已启用 `mjpeg`,
  **需要重新跑一遍 ffmpeg 编译脚本**)→ sws 转 RGB565 写入常驻 staging →
  置 ready;主线程 `thumb_get()` 看到 ready 才做 GPU 上传
  (GX 传输队列非线程安全,上传必须在主线程)
- 纹理 20 张 128x64 RGB565 **常驻复用**(共 320KB linear),翻页只换内容
  不重建;未就绪画灰色占位块,加载完自然浮现
- 翻页/进播放器时 `thumb_stop()`(退出标志 + join),播放期间不与视频
  抢带宽;回列表自动续载
- **SD 卡封面缓存**(`sdmc:/3ds/3danmu/thumbs`):原始 JPEG 存盘,
  下次同一封面直接读盘。这是封面提速唯一有效的手段 —— 实测单张 2.3KB
  却要几百毫秒,且 `rd=0 fb=0`(无重定向、无 TLS),纯粹是 3DS 网络栈的
  往返延迟,**加线程、加并发、调优先级都无效**。SD 读 2KB 是毫秒级,
  快两个数量级。翻回上一页、退出播放回列表、重开 App 全部秒出。
  文件名是 URL 的 64 位哈希;超过 100MB 自动整体清空
  (不做 LRU:记访问时间要每次读写都改 mtime,那又是 SD 开销;
  按 2.3KB/张算 100MB 能存四万张,真到那步清空重来完全可接受)。
  设置页有手动清理按钮,上面直接显示当前占用
- mjpeg 解码器缺失(老版 ffmpeg 未重编)时安静降级为无封面,不影响功能
- **多线程/多核提速无效,别再试了**。两个 loader 线程本来就跑在
  core2/core3 上,是真的分核 —— 但所有 HTTP 请求最终都要过 **httpc
  系统服务**,它是共享的、内部本就串行的,CPU 核变不出网络吞吐。
  实测把请求改成真并发(3 路):**速度毫无改善**,而且**字幕串台复发**。
  零收益、有代价。瓶颈是每请求的固有延迟,只能靠「不发请求」(缓存)解决
- **解码上下文按线程复用,不是每张图新建一套**。原先每张封面都要
  `avcodec_alloc_context3` + `avcodec_open2` + `sws_getContext`,用完全销毁——
  这三件事都是大块 malloc 加建表(mjpeg 的 VLC 表、swscale 的缩放系数表),
  一张图一轮。**newlib 的堆是全进程一把锁**,两条 loader 同时在那里申请释放,
  主线程只要碰一次分配就得排队,表现就是滚动中「偶尔卡一小下」。
  封面尺寸固定(图床按 `@96w_60h` 返回),上下文一直用下去即可,
  只在源尺寸真变了才重建缩放表。上下文放在 `loader_main` 的栈上
  (**不能用 `static`**,两条线程会打架)
- 老机型只开 **1 条** loader:没有 core2/3 时两条都会回落到主线程那颗核上,
  互相抢还连累 UI,不如老实串行
- **层级 = 深度值(GEQUAL),不是绘制顺序**。曾误判 citro2d 关闭了深度
  测试、想靠"最后画顶栏"来遮挡——真机立刻打脸:行文字(z=0.5)照样穿透
  后画的顶栏(z=0.4),选中高亮(z=0.4)反而盖掉缩略图(z=0.2)。
  实际规则:**z 大者在上,同 z 后画者胜**。全工程层级约定:
  视频 0.1 < 选中高亮 0.15 < 封面占位 0.18 < 封面图 0.2 < 普通色块 0.4
  < 文字/弹幕 0.5 < 悬浮框底 0.6 < 悬浮框文字 0.7。
  要盖住文字的东西 z 必须 > 0.5(顶栏、缓冲提示框都是)
- **滚动防抖三件套**:行坐标必须取整(小数 y 会让文字在像素间跳动,
  肉眼就是"发抖");封面 GPU 上传每帧限 1 张(`C3D_SyncDisplayTransfer`
  是同步等待,一帧连传几张必掉帧);**输入写目标值、显示值每帧缓动 35%**
  ——直接改显示值的话,十字键把选中项顶出边缘时会单帧瞬移一整行。
  但只做缓动还不够:逐行连发即使平滑了也是节奏性的"软跳"。最终方案是
  **单击移一行、按住 300ms 后切入连续滚动**(视口匀速 4.5px/帧,
  选中项贴边跟随),和摇杆一样丝滑,这才是官方 App 的手感
- **下屏底部有状态条**:显示当前动作、封面加载进度,以及**上次失败的原因**
  (取流失败、cid 查询失败等,常驻到下次成功加载才清)
- **选中行标题跑马灯**:过长标题停 1.2s 后缓慢滚动,到尾停顿再回起点。
  依赖 GPU 裁剪(`ui_clip`):3DS 帧缓冲相对屏幕旋转 90°,scissor 坐标要按
  `(240-y2, W-x2, 240-y, W-x)` 换算;citro2d 攒批渲染,改 scissor 前必须
  `C2D_Flush()` 把已攒的批画掉,否则裁剪会错误地作用到之前的内容上
- **排版基准**(两轮调大后的约定):按钮字 0.7,列表标题 0.62,正文 0.52-0.6,
  提示小字 0.46-0.5,标题 0.58-0.6;按钮高统一 40px。改布局时先画在纸上
  把每个元素的 y 区间列出来查重叠,别指望肉眼

---


---

## 触屏拼音输入法

3DS 系统键盘只有中国大陆机型(神游版)带拼音,日/美版只能敲英文——
所以自己写了一个:下屏 QWERTY 触屏键盘 + 拼音词库检索 + 候选栏,
在任何机型上都能输入中文。

### 使用

搜索时自动弹出。敲拼音 → 候选栏点词上屏(或**空格选第一候选**),
**数字排常驻键盘顶部**;`中/英` 与 `符` 是两个独立开关
(符 = 字母区切成三排符号键面,数字任何模式可用),
`删除` 先删拼音再删文本,L/R 翻候选页,
B 清拼音(无拼音时=取消),START 或`完成`确认。
支持连续拼音:敲 `bilibili` 这种多音节串,候选按"整串匹配优先、
部分匹配兜底"给出,选词后消耗对应字母、剩余继续拼。

### 词库(tools/gen_pinyin_dict.py)

- 数据源:**jieba** 的词频表(35 万词)+ **pypinyin** 标音,
  取频率最高的 4.2 万词(≤4 字)+ 全部 1.1 万单字,共约 5.4 万条、
  生成 `romfs/pinyin.dic` 约 1MB(仓库已带,不用重新生成)
- 格式的关键设计:**构建期按(拼音升序、同拼音词频降序)排好序**。
  运行期前缀检索 = 二分找区间起点 + 顺序扫描,扫出来天然就是按词频
  排好的候选——不需要运行时排序,也不用存词频字段
- 检索策略:先收"拼音以整个输入串开头"的词(选中消耗全部输入),
  再按前缀长度从长到短收"拼音恰等于前缀"的词(选中消耗部分输入),
  正好覆盖"整词联想"和"逐词上屏"两种打字习惯
- ü 按主流输入法习惯写作 v(绿 = lv)
- 重新生成:`pip install jieba pypinyin && python3 tools/gen_pinyin_dict.py romfs/pinyin.dic`

词库缺失时(romfs 里没放 pinyin.dic)输入法自动退化为纯英文键盘,
功能不受影响,只是打不了中文。

---


---

## 中文字体

上屏 UI 用 citro2d 渲染,字体来自 **3DS 中文系统字体**
(`C2D_FontLoadSystem(CFG_REGION_CHN)`,失败则台版 → 默认日版)。

### 为什么不用像素字体(踩完坑的结论)

直觉上点阵字体应该更适合小屏:每一笔都落在整像素上,没有抗锯齿的灰边。
实际试了 Fusion Pixel 的 8px / 10px / 12px 三个版本,结论是**不通**。

关键在于点阵字体只有 **1.0 倍绘制**才不失真 —— 一旦缩放,LINEAR 会把
边缘混成半透明灰(发虚),NEAREST 会直接丢像素(缺笔画),两头都是坏的。
所以字号必须靠 `mkbcfnt -s` 在转换时定死。而 12px 版本转出来,汉字实际
只有 **11 像素高**,这个高度画不下复杂字:

```
国 →  .###########.       边框闭合,完全正确
      .#.#######.#.
      .#....#....#.

赢 →  .###########.       十七画塞进十一行,底部退化成点阵
      .#.#.#.#.#.#.
      .###.#.#.#.#.
```

**这两张图是从图集纹理里直接挖出来的,不是屏幕截图** —— 也就是说纹理里
就已经这样了,和绘制、过滤器、坐标对齐统统无关,是物理上限。
换 `-s`、换 8px/10px(更小只会更糟)都无解。

要点阵又要笔画完整,只能上 16px 字体,代价是一屏少放约 40% 内容。

抗锯齿字体在这个尺寸反而更容易认:灰阶能表达「这里有笔画、偏左半个像素」,
二值点阵只能非黑即白。所以最终退回系统字体。

> **教训**:排查「字糊」时不要靠肉眼猜,先用 `tools/fontdump.py` 把字形从
> 图集里挖出来看。我们一度用「中间灰占比 0%」当作「转换正确」的判据 ——
> 那是错的:mkbcfnt 按单色阈值光栅化时输出**永远**是纯二值,缩小丢笔画
> 时也是 0%,这个指标恒等于 0,什么都没测出来。

### 诊断工具

```bash
# 看某个字在图集纹理里到底长什么样(最有用的一个)
python3 tools/fontdump.py romfs/font.bcfnt --char 永国播赢

# 看整张图集的概况和灰阶分布
python3 tools/fontdump.py romfs/font.bcfnt

# 扫 mkbcfnt 各个 -s 档位的输出尺寸
python3 tools/fontsweep.py 某字体.ttf 4 12
```

判据是**纹理和屏幕对比**:屏幕上比纹理里更缺 → 绘制端有问题;
两边一样 → 字体/尺寸的问题,绘制端再怎么调都没用。

### 想重新试点阵字体

代码里留了开关,`source/ui.c`:

```c
#define UI_USE_ROMFS_FONT 0    /* 改 1 则加载 romfs:/font.bcfnt */
```

打开的同时**必须**把 `comment.c` 的 `CM_FONT`/`CM_META` 和 `ui.c` 的
`LOG_FONT` 改回 `1.0f`,并把 `UI_FONT_FILTER` 改成 `GPU_NEAREST` ——
否则点阵会被缩放,比现在更糟。字号靠 `mkbcfnt -s` 调,不要靠倍率。
要更大用 **2.0**(整数倍放大仍锐利),**不要 1.5**。

### 字号常量

系统字体的格子约 30px 高,所以 `0.5` ≈ 15px 汉字。全工程的字号都在
0.42~0.8 这个区间,改动前先和相邻页面对齐:

| 位置 | 常量 | 当前值 |
|---|---|---|
| 评论区正文 | `comment.c` `CM_FONT` | 0.5 |
| 评论区用户名/赞数 | `comment.c` `CM_META` | 0.45 |
| 调试台 | `ui.c` `LOG_FONT` | 0.45 |
| 列表/播放器等 | 各处字面量 | 0.42~0.8 |

行高、栏高、按钮高、折行宽度**全部由 `ui_text_height()` 实测推导**,
不要硬编码 —— 改字号或换字体时布局会自己适配。

搜索输入用的是**自带的触屏拼音输入法**(见下),不依赖 3DS 系统键盘,
任何区域的机型都能打中文。

---


---

## MVD 硬件解码完全指南

New 3DS 独有的 MVD(Media Video Decoder)硬件 H.264 解码器,公开资料极少,
本项目参考 [Core-2-Extreme/Video_player_for_3DS](https://github.com/Core-2-Extreme/Video_player_for_3DS)
的实现跑通了**含 B 帧的在线流**。以下是完整流程与全部坑点。

### 正确的调用流程

**1. 计算工作缓冲大小**(不要用默认值!)

```c
MVDSTD_CalculateWorkBufSizeConfig c = {0};
c.level.enable = 1;
c.level.flag = MVD_CALC_WITH_LEVEL_FLAG_ENABLE_CALC
             | MVD_CALC_WITH_LEVEL_FLAG_ENABLE_EXTRA_OP
             | MVD_CALC_WITH_LEVEL_FLAG_UNK;      // 三位全开
c.level.level = mvd_level_map(vpar->level);       // ffmpeg level → MVD 枚举
c.width = vw; c.height = vh;
mvdstdCalculateBufferSize(&c, &wsize);            // 480P 约 6.8MB
```

浏览器默认的 `MVD_DEFAULT_WORKBUF_SIZE`(约 9MB)看似更大,
但结构不对,**装不下 B 帧所需的参考帧集合,会导致画面残影**。

**2. 初始化与配置(宽高都要 16 对齐)**

```c
mvdstdInit(MVDMODE_VIDEOPROCESSING, MVD_INPUT_H264, MVD_OUTPUT_BGR565, wsize, NULL);
int vw_al = (vw + 15) & ~15, vh_al = (vh + 15) & ~15;   // 360 → 368
mvdstdGenerateDefaultConfig(&cfg, vw_al, vh_al, vw_al, vh_al, NULL, NULL, NULL);
```

高度不对齐会让渲染**静默失败**(返回成功但一个字节都不写)。

**3. 预热 SPS/PPS**

从 avcC extradata(mp4 的 `codecpar->extradata`)里取出 SPS 和 PPS,
各自加 3 字节 `00 00 01` 前缀单独送一次。

**4. 每个数据包**

```c
// 输出缓冲四角写标记,用来检测"有没有写入"
mark_corners(out, 0x11);
cfg.physaddr_outdata0 = osConvertVirtToPhys(out);
MVDSTD_SetConfig(&cfg);

// avcC(4 字节大端长度 + NAL)→ annex-b,整包所有 NAL 拼接后一次送入
mvdstdProcessVideoFrame(buf, len, 0, NULL);
if (first_packet) mvdstdProcessVideoFrame(buf, len, 0, NULL);  // 首包送两次

if (corners_changed()) {
    got_frame = true;
    skip_next_process = true;   // 内部还有排队帧,下个包先排空
} else {
    // 没出帧:排空渲染队列
    for (;;) {
        Result r = mvdstdRenderVideoFrame(&cfg, false);   // 非阻塞
        if (corners_changed()) { got_frame = true; break; }
        if (r != MVD_STATUS_BUSY) break;
    }
}
```

**5. B 帧重排**

MVD 内部有重排队列,输出顺序 ≠ 输入顺序。处理方式:
Process 出帧后置 skip 标志,下一个包**先不送**,而是调用 Render 排空内部队列;
排空无果再送包。时间戳用 **dts 的 FIFO 队列**(解码顺序单调)与输出帧配对。

**6. 读取输出:等 DMA 收尾**

角标变化只说明"帧开始到达",DMA 尾部可能还在写。用**双缓冲**,
并把读取推迟到主线程呈现时(至少一个邮箱周期之后),
否则会读到半新半旧的帧(残影)。

### 把 mvd 系统模块搞崩(整机重启才能恢复)

实测崩溃现场(Luma 异常界面):

```
Exception type:   prefetch abort (svcBreak)
Current process:  mvd (0004013000004102)
```

**崩的不是本程序,是 3DS 的 MVD 系统模块。** 一旦崩掉必须重启主机。
复现路径:**画面已经卡住 → 用户反复拖进度条**。

原因:每次 seek 都要 `mvdstdExit()` + `mvdstdInit()` 重开解码器,
连续快拖就是一轮接一轮的 Exit+Init。而画面卡住时 MVD 本来就处于异常状态,
此时**最危险的事就是继续戳它** —— 偏偏用户看到卡顿的本能反应就是拖进度条。

三层防护:

1. **重开节流**:距上次重开不足 400ms 就跳过。不重开的代价只是解码器里
   可能残留旧帧,而那些帧会被 `seek_gen` 判为上一代、根本不会上屏
2. **Exit 前后各留沉降时间**(20ms / 30ms):硬件可能还在 DMA 上一帧,
   给它收尾时间再拆
3. **卡死就主动降级软解**:连着 60 个包(约两秒)一帧不出,
   直接 `ret = -99` 重开为软解。慢一点,但根本不会走到那条危险路径上

#### 解码器热切换:在 seek 时机原地换,不重播

早先「MVD 卡死」的处理是 `ret = -99` 整段重播,而重播会**把进度清零** ——
看到一半被拽回开头,比卡一下更难受。

现在改成**原地热切换**:worker 发现 MVD 卡死时,发起一次「跳到当前位置」的
seek,真正的换解码器动作在 seek 处理里完成。

**为什么必须在 seek 时机切**,三个理由缺一不可:

- seek 本来就要冲刷解复用器、解码器、音频队列,**换解码器需要的清理它全做了**
- 画面本来就要中断一下,切换不额外增加可见干扰
- seek 落在关键帧上,新解码器立刻有参考帧可用(否则要等下一个 IDR,好几秒黑屏)

#### 切回硬解:后台试运行,验证通过再换显示源

难点在于**没有任何信号能说明「MVD 现在好了」** —— 唯一的检验方式
就是去初始化它并喂数据。所以关键是让这次检验**不影响正在播的画面**。

办法:MVD 是硬件模块、软解是纯 CPU 的 ffmpeg,**两者完全独立、可以并存**。
于是降级满 10 秒后:

1. 后台 `mvdstdInit`,软解**照常出画**,用户毫无察觉
2. 把**同一个包**同时喂给 MVD,只看它出不出帧
   (试运行期间跳过像素搬运,省掉每帧几百 KB 的拷贝)
3. 连着出够 **10 帧**才认定它恢复正常,此时把显示源切过去、拆掉软解
4. 喂满 150 个包仍不达标就收摊(`mvdstdExit`),本次播放不再试

这样切换点**完全不可见**:不用 seek、不中断画面。更重要的是
「MVD 好没好」是**验证出来的,不是赌出来的** —— 万一它还是坏的,
坏的只是后台那份,用户看到的画面自始至终由软解稳稳供着。

> 冷静期不能省:刚出过问题就立刻重新初始化,正是把系统模块彻底搞崩的路径。
> 取 10 秒 —— 敢压这么短是因为**试运行本身不影响画面**,
> 万一 MVD 还是坏的,坏的也只是后台那份。
> 跳转会打断包的连续性,所以 seek 时试运行直接作废重来。

一个容易漏的实现细节:`mvd_stop()` 的判据必须是「**初始化了没有**」
而不是「**现在用不用它**」—— 试运行期间 `use_mvd` 还是 false(画面归软解),
但 MVD 确实已经 init 过了,按 `use_mvd` 判断会漏掉 `mvdstdExit`,
把系统模块晾在那儿。

**降级不是一降到底。** 软解只在「本次播放」内生效,换个视频重新试硬解 ——
MVD 卡住多半是这一条流、这一次的偶发状况,没理由让后面所有视频都陪着吃软解
(软解只能 360P 且掉帧,体验差很多)。硬解正常跑完一个视频就把失败计数清零。

但**连续 3 个视频都失败就本次运行不再试**:那多半是 mvd 系统模块本身
状态已经不对,而反复初始化一个不正常的系统模块正是把它彻底搞崩的路径。
自动恢复要有,但不能无限重试 —— 这和上面「重置不一定安全」是同一条道理。

> 教训:**当一个子系统已经处于异常状态时,「重置它」不一定是安全操作。**
> 尤其它是系统级共享资源、崩了要整机重启的时候 ——
> 宁可降级到一条更慢但完全独立的路径(软解),也不要反复重置。
> 而且要考虑用户的本能反应:卡了就会去拖进度条,那条路必须是安全的。

### 全部踩坑记录

| 坑 | 现象 | 解法 |
|---|---|---|
| 工作缓冲用默认值 | 画面残影,运动越大越糊 | 用 `mvdstdCalculateBufferSize` 按级别算 |
| 尺寸未 16 对齐 | 渲染返回成功但不写数据 | 宽高都对齐到 16 |
| 逐个 NAL 送入 | 花屏 | 整个 AU 的 NAL 拼接后一次送 |
| 只在 FRAMEREADY 时渲染 | 黑屏 | `MVD_STATUS_OK` 也要渲染 |
| 大 NAL 一次没吃完 | 关键帧只解半张 | 收到 `INCOMPLETEPROCESSING` 用 `remaining_size` 续传 |
| 每个 slice 都渲染 | 半张新半张旧 | 一帧的所有 slice 送完后只渲染一次 |
| 渲染后立刻读缓冲 | 底部残影 | 双缓冲 + 推迟到呈现时读 |
| CPU 写 VRAM | data abort 崩溃 | HBL 环境下 VRAM 对 CPU 只读 |
| worker 线程调 `GX_TextureCopy` | `svcBreak` 崩溃 | 用 CPU memcpy,或在主线程做 |
| libctru 自动递增 frameid | 多 slice 的帧 id 错位 | 同一帧的所有 NAL 用同一个 frameid |

### 已排除的错误方向(省得重走)

输出缓冲放 VRAM(不需要)、SetupOutputBuffers 槽位机制(不需要)、
ProcessNALUnit 的 flag 参数(保持 0 即可)、瓦片/Morton 布局解读(输出就是线性)、
延迟一帧渲染(不需要)。

---

---


---

## 3DS 开发踩坑记录

与 B 站无关、但对任何 3DS 自制程序都适用的经验:

**HOME→X 卡在 "Closing software":不是一个 bug,是一串等待的总和**
系统关闭应用的前提是**主线程回到 aptMainLoop 并看到 false**。主线程被任何
东西拖住,系统就只能干等。实测这一个症状底下叠着五层,修一层露一层:

1. 主线程卡在同步 HTTP 请求里(httpc 无超时)→ 掐连接(见下条);
2. 各后台线程的 threadJoin 按秒计超时,几个叠加近半分钟 → 关门时压到 0.5s;
3. `mvdstdExit` 内部有**无上限**的忙等(`while (BUSY) ControlFrameRendering`),
   解码器停在半帧上就永远转 → 进程将亡时直接跳过它,会话随进程回收;
4. 播放器从主循环某一格里返回后,**本轮循环剩下的部分还会照常画一帧**,
   而退出状态下 GSP 的 VBlank 可能永远不来 → 返回点直接 break,别画;
5. `APTHOOK_ONEXIT` 由 APT 事件线程分发,和主线程冲出子循环之间是**竞态**
   (实测两次 HOME→X,一次钩子先到、一次没到)→ 关键路径不赌钩子,
   自己调 `aptShouldClose()` 同步问。

诊断手段就一个:退出路径每一步同步落盘一行(异步日志在进程被杀时来不及
写)。日志停在哪两行之间,锅就在那一段。修完的判据也干净:从 join 到
gfxExit 全链 190ms。

**"自愈重建服务" 必须知道谁还在用这个服务**
httpc 服务态坏掉后用 `httpcExit + httpcInit` 重建,是个有效的自愈手段 ——
但 `httpcExit` 会把共享内存和所有会话一起拆掉,**别的线程要是正在
httpc 的调用体内,就崩在 libc 的 free 里**(数据异常、写权限错,
调用链 `httpc_reset → httpcExit → _free_r`,用 addr2line 对 elf 一查便知)。

防护计数还不能只记"连接开着":断线重连期间连接是**关着的**,计数为 0,
可下载线程正卡在 `httpcOpenContext/BeginRequest` 里面。要再加一个
「有线程在调用体内」的进出计数,两个都为零才允许重建。
教训浓缩成一句:**豁免名单要按"谁在用服务"记,不要按"资源是否存在"记。**

**httpc 的请求既没有超时,也不认任何标志位**
唯一能从外面打断在途请求的手段是 `httpcCancelConnection()`,而且它**可以
从另一个线程调**。想让 HOME/睡眠不被网络拖住,就只能登记在途的
`httpcContext` 然后掐掉它 —— 设个"请退出"的 volatile 标志是没用的,
下载线程根本没机会看那个标志,它卡在 IPC 里。

两个配套的坑:

- 掐掉之后调用方拿到的是普通错误码,和真正的失败分不开。要在业务层
  记住"这次是我自己掐的",否则会把它当成永久失败处理(封面就此少一张)。
- 登记表要和关闭路径**同生共死**。`do_request_ka` 有 7 个出口,漏掉任何
  一个,登记表里就留下一个指向已经出栈的 `httpcContext` 的指针,
  下次取消时对着野指针发 IPC。做法是把所有出口收敛到一个
  `close_ctx()` 里,再也不直接写 `httpcCloseContext`。

**CIA 想用 MVD 硬解:exheader 的内核版本必须 >= 2.44**
3dbrew 的 MVD_Services 页面写着 "the process must have kernel-release-version
>=2.44"。RSF 里的 `ReleaseKernelMinor` 如果沿用模板里的 `"33"`,mvd 会
**先让你以为成功**:`mvdstdInit` 返回 `00000000`,然后第一次
`mvdstdProcessVideoFrame` 就在 mvd 进程内 svcBreak。

```
ReleaseKernelMajor : "02"
ReleaseKernelMinor : "44"     # 不是 "33"
```

判据非常干净:**崩溃和喂进去的数据完全无关**。换视频、换 NAL 长度
(实测 31B / 34B / 12818B 三种)、改喂法(单独送参数集 vs 拼进首帧),
崩溃点 PC、LR、SP 一个字节都不变,只有那个"长度"寄存器跟着变。
数据无关的崩溃就别再往数据上找原因 —— 是环境不满足前置条件。

我在这上面绕了两圈:先猜"输出缓冲没配置",又猜"参数集不能单独送"
(后者被 devkitPro 官方 mvd 例程直接证伪,它就是一个 NAL 一个 NAL 喂的)。
两次都是拿着数据侧的假设去解释一个数据无关的现象。

`.3dsx` 经 Homebrew Launcher 启动时继承宿主的 exheader,版本号是够的,
所以这个坑只有 CIA 会踩。

**libctru 的 `mvdstdProcessVideoFrame` 少转了一次地址窗口**
`linearAlloc` 返回的是**旧 FCRAM 窗口**地址(`0x14xxxxxx`)。New3DS 上也是
如此 —— 看到 `0x14` 开头**不**代表 exheader 退回了 Legacy。但 MVD 是 New3DS
模块,它按**新窗口**(`0x30xxxxxx`)解释传进去的虚拟地址。

libctru 自己知道这件事,却只做了一半:

```c
/* mvdstdInit:工作缓冲转了 */
MVDSTD_Initialize((u32*)osConvertOldLINEARMemToNew(mvdstd_workbuf), size);

/* mvdstdProcessVideoFrame:物理地址转了,虚拟地址原样透传 */
MVDSTD_ProcessNALUnit((u32)inbuf_vaddr, (u32)osConvertVirtToPhys(inbuf_vaddr), ...);
```

于是 mvd 拿着 `0x14xxxxxx` 在自己的地址空间里解引用,那是片空地 →
**mvd 进程 data abort**。调用方看到的是 Luma 崩溃页写着
`Current process: mvd`,而 **FAR 恰好等于自己那块输入缓冲的地址**——
这是最好认的指纹:把缓冲地址打进日志,和 FAR 一对就知道了。

修法是自己先转好再传:

```c
p->mvd_in_n3 = osConvertOldLINEARMemToNew(p->mvd_in);
mvdstdProcessVideoFrame(p->mvd_in_n3, len, 0, NULL);
```

`osConvertOldLINEARMemToNew` 走的是「虚拟→物理→加 0x10000000」,所以
libctru 内部再对结果做一次 `osConvertVirtToPhys` 仍得到同一个物理地址,
两个参数就都对了。

顺带一提,为什么 `.3dsx` 从来没崩:两种打包方式的线性堆落点不同,3dsx
那个野地址碰巧落在 mvd 已映射的页里,读到垃圾但不触发异常。**"另一种
打包方式不崩" 不等于 "那种打包方式没这个 bug"**,只是没触发而已。

**`0xD040xxxx` 是"忙",不是"不支持"**
`mvdstdInit` 返回 `0xD0401834` 时一度以为是硬件不认。拆开看:
level = Temporary、summary = WouldBlock,和 libctru 里
`MVD_STATUS_BUSY`(`0xD0406B03`)同族 —— 意思是"现在不行,等会儿再来"。
常见成因是上一次会话没干净收尾(mvd 崩过,或进程被强杀导致
`mvdstdExit` 没跑到),硬件还挂在别人名下。隔 150ms 重试几次即可;
仍然忙就是彻底卡住了,只有重启主机能救。

碰到没见过的 Result,先按 level/summary/module/description 拆位再下结论:

```
bits 31-27 level | 26-21 summary | 17-10 module | 9-0 description
```

**给系统模块小于它要求的缓冲 = 把它搞死**
`mvdstdCalculateBufferSize` 算出多少就必须给多少。曾经为了省内存做过
「按剩余内存削顶」和「init 失败就减半重试」,两个都会让 MVD 卡死整机。
内存不够时唯一安全的做法是**放弃硬解、回软解** —— 慢是可以接受的,
把用户的机器锁死不行。

**detached 线程 ≠ 可以不等它结束**
封面缓存的目录扫描线程当初建成 detached,注释还写着"不用回收"。
detached 只是说不必 `threadFree`,**不代表退出时可以不管它**:
按 START 退出后 `main` 返回,运行时把文件系统拆掉,而它还卡在
`readdir`/`stat` 里 —— data abort,读 NULL+0x44,core 3。
凡是做文件或网络 I/O 的线程,退出前必须置标志 + `threadJoin`。

**清理的判据是「我有没有拿到它」,不是「整件事成没成」**
音频初始化一进来就 `ndspInit()`,但 `audio_ok` 要等解码器 + NDSP + 重采样器
**全部**就绪才置位;而清理写的是 `if (audio_ok) ndspExit()`。
于是只要中间任何一步失败(线性内存被 MVD 吃光时就会),
`ndspInit` 调过、`ndspExit` 没调,**同时缓冲区照样被释放** ——
DSP 还在跑、通道的 wavebuf 指向已释放内存,
表现是**从那次之后所有视频都没声音**,重启才好。
每个"获取"动作要有自己的标志位,和"整体是否可用"分开。
释放顺序也不能反:先停通道,再放缓冲。

**MVD 工作缓冲要不到,现象却是"硬解不可用"**
`mvdstdCalculateBufferSize` 失败时如果退到 `MVD_DEFAULT_WORKBUF_SIZE`
(0x9006C8 ≈ **9.4MB**,那是 level 5.2 的量),线性堆总共才十来 MB,
`mvdstdInit` 直接把内存吃光,后续 `linearAlloc` 全部失败。
日志上只看到"硬解起不来",很难联想到是缓冲开大了。
正确做法是**按分辨率反推 level**(360P/480P → 3.0,约 2~4MB),
并且在日志里把 `workbuf` 和 `linearSpaceFree()` 一起打出来。

**失败计数器要想清楚什么时候清零**
硬解连续失败 N 次后禁用是对的,但"连续"的判定当初写成了
「整段视频播完时仍在硬解」——中途热切软解、或跳转时 MVD 重置失败,
收尾时都不满足,于是计数只增不减,三次之后本次运行**所有**视频都是软解。
判据应该是"硬解这次真的出帧了"(出满 30 帧即清零),而不是"这段是怎么结束的"。

**newlib 的 printf 不支持 `%lld`**
64 位数字要手动转字符串,否则输出乱码(会导致 URL 里的 cid 变成垃圾字符,
服务器返回 -400)。

**主线程默认栈只有 32KB**
ffmpeg 解码调用链很深,必须加大:

```c
unsigned int __stacksize__ = 256 * 1024;
```

大结构体(含缓冲区)绝不能放栈上,用 `static` 或堆分配。

**httpc 上下文有线程亲和性**
在 A 线程建立的连接,到 B 线程读会**永久挂起**(不报错、不超时)。
必须在使用它的线程里重新建立连接。

**CDN 可能忽略 Range 请求**
返回 200 + 完整文件(而不是 206 + 片段),此时必须手动丢弃前 N 字节,
否则解封装读到错位数据(`Invalid NAL unit size`)。

**console 和 GPU 渲染会打架**
`consoleInit(GFX_BOTTOM)` 会把下屏改成 RGB565 单缓冲,
与 citro2d 的 BGR8 双缓冲冲突 → 花屏。切换时要:
`gfxSetScreenFormat` + `gfxSetDoubleBuffering` 还原,
并且**不能在一帧绘制中途切换**(帧末 GPU 会把旧格式画面怼上去)。

**console 只支持 ASCII**
中文日志会变乱码。UI 文字用 citro2d 画,日志一律英文。

**linear 内存很紧张**
ffmpeg + 1.5MB 环形缓冲 + MVD 工作缓冲(6.8MB)+ 视频纹理 + 帧缓冲
很容易耗尽。分配失败时 `linearAlloc` 返回 NULL,要检查。

**线程退出要让所有等待循环都能感知**
`threadJoin(U64_MAX)` 遇上卡在"等网络数据"的循环 = 整机卡死。所有
`while(...) svcSleepThread(...)` 都要检查退出标志;长退避睡眠要拆成
小段(如每 100ms 检查一次);join 加超时兜底;退出顺序为
"置 quit → 停下载线程(让 AVIO 立刻返回 EOF)→ join worker"。

**NDSP 通道的采样位置计数不随 WaveBufClear 归零**
做 seek 一类的时间轴跳转时,必须 `ndspChnReset` 重置通道并重新配置参数,
否则以 `ndspChnGetSamplePos` 为基础的时钟会累积误差。

**"清理函数"里 join 线程,会让线程自己等自己**
如果加载函数内部调用了带 `threadJoin` 的清理函数,而这个加载函数又跑在
被 join 的线程里,就会死锁。把"清数据"和"清线程"拆成两个函数是简单可靠的解法。

**环形队列的边界要按"满"和"空"两种极端验算**
队列满时 `head == (head + len) % CAP`,任何"退回队首"式操作都会与写入队尾
撞在同一槽位。这类 bug 不会当场崩,而是留下悬垂指针,在几次操作之后才发作
(本项目就是"第二次拖动进度条才崩")。

**crash dump 会说话**
崩溃屏上的 `Fault status` + `FAR`(出错地址)+ `Access type` 三件套
基本能定位问题:栈溢出(FAR 在 SP 附近)、权限错误(写 VRAM)、
空指针(FAR 接近 0)。

---

### httpc 服务被并发打坏:串行化 + 自愈

曾出现过一次"搜索正常,之后所有请求一律 `http error`(传输层失败,
不是 API 拒绝)"的整片故障。事后复盘更可能只是 Wi-Fi 不稳,但当时也
暴露了一个真实风险:弹幕线程与主线程会同时向 httpc 发短请求,而 3DS 的
httpc/sslc 服务对并发短连接并不健壮。以下措施作为保险保留(正常路径
零开销,只在失败时起作用):

1. **短请求全部串行化**:`net_get` / `net_post_form` 过同一把 `LightLock`。
   短请求最多几百 ms,串行的代价可忽略;视频流(NetStream)有自己的
   长连接和专属线程,不走这把锁
2. **自愈**:请求失败时打印失败在哪一步(`net fail step2` = OpenContext,
   3 = BeginRequest,4 = 读状态码,6 = 下载),然后 `httpcExit()` +
   `httpcInit()` 重建服务会话,再重试一次。有视频流活跃时跳过重建
   (会拔掉正在播放的连接),靠活跃流计数(`net_note_stream`)判断,
   计数在 open/close/重连/seek 所有路径上都维护了

---

## 两条从网络层学到的通用经验

下一节说明了为什么接口细节不再收录。但其中有两条**与任何具体平台无关**
的教训值得留下 —— 它们在任何带异步和远程调用的程序里都成立。

### 「跳过这次操作」和「让这次操作无效」是两件事

一个用代际计数(generation counter)防串台的异步加载器,曾经修好又复发,
而复发**是我为了修另一个 bug 亲手造成的**:

当时发现渲染循环会被加载函数里的 `threadJoin` 卡住,于是加了一句
`if (s_loading) return;` 直接返回。看着只是「这次先不拉」,
实际上**代际没有推进** —— 上一轮还在跑的线程回来时
`mygen == s_gen` 依然成立,于是把上一个对象的结果发布到了当前对象上。

正确的顺序是**先推代际,再决定要不要返回**:

```c
s_gen++;              /* 无论如何,在途结果一律作废 */
__dmb();
if (s_loading) return false;   /* 不等,但也绝不让旧结果生效 */
```

同时函数要返回 bool —— 被推迟时调用方不能把它当成「已经做过了」,
否则这个对象就再也不会重试。

> 带代际/版本号的异步结构里,**任何提前返回都要先问一句:
> 在途的那些结果,现在还算数吗?**

配套的第二条:**校验点要放在离结果最近的地方**。
先前校验的是「元数据响应对得上」,但真正决定内容的是后续那个资源 URL ——
中间隔了几环,就有几处可能串。最终闸门要贴着最终结果。

### 把假设编码进程序,让它自己在真机上做实验

远程调试时,「改一版、发一版、问一次」的循环极慢。更快的做法是
**把几种候选方案排成阶梯写进程序**,让它依次尝试并把「哪条通了」
打进日志;成功的那条记住,下次直接用,失效了自动忘掉重新探。

一次能拿回的信息量,比「这次还是不行」多得多。

---

## 关于网络层与接口

本文**不收录接口清单、签名算法、参数结构和风控应对**的记录。

原因不是技术上不值得写 —— 恰恰相反,那部分是这个项目花时间最多的地方。
是因为把这些「系统性收集、整理」出来的东西以文档形式公开传播,
在国内已有平台方发律师函要求下架的先例(2026 年 1 月,
一个长期存在的接口文档项目因此关停)。本项目**不想成为那样的传播渠道**。

需要了解实现的,直接读 `source/bili.c` 与 `source/wbi.c`。
代码在,注释也在;只是不再单独整理成一份可供检索的文档。

**这不影响使用本程序**,也不影响本文其余部分 ——
后者绝大多数与哔哩哔哩无关,是 3DS 平台本身的经验。

> 顺带一提:接口随时会变。真要维护这一层,读当下的代码和调试台输出
> 比读一份注定过期的文档可靠得多。


## 调试方法

### 调试台:为什么是自绘的,以及它自己踩过的坑

用法在 [README](README.md#调试台) 和 [测试指南](TESTING.md) 里,
这里只记设计取舍和踩过的坑 —— 后者都是**「排查工具本身骗了排查的人」**
这一类,比被排查的 bug 更难发现。

**日志按屏幕宽度折行(54 字节),不是按缓冲区大小。**
早先存 110 字节一行,而下屏只画得下约 54 个 —— 超出部分被
`ui_text_clipped` 截成「...」,右半截根本看不见。排查那几轮里反复出现的
「这行应该打了却没看到」,有一部分就是这么来的:
**日志被自己的显示层吃掉了,而排查的人还以为代码没执行。**
折行只在 UTF-8 字符边界处断,免得半个汉字变成乱码方块。

**同一个坑踩了两次:忘记 `#include "ui.h"`。**
那个头文件里的 `#define printf ui_printf` 才是把日志送进环形缓冲的东西。
`thumb.c` 长期漏了它,于是封面模块的所有 printf(包括「mjpeg 解码器缺失」
这条重要警告)**从来就没在调试台里出现过**。`net.c` 早先踩过一模一样的坑。
凡是新增的 .c 文件,记得 include ui.h。

同类的坑还有两个:

- 缓冲原本只有 160 行,**启动阶段的日志最关键却最早被冲掉**(现在 400 行)
- 探针挂在「第一次请求」上自动打印,而第一次请求发生在被观测的动作
  **之前** —— 快照取早了,永远看不到要看的东西

> 三件事指向同一条:排查时若反复出现「应该有却没有」,
> **先怀疑观测手段本身,再怀疑被观测的代码。**

**不用 3DS 自带 console,是因为屏幕格式会打架。**
`consoleInit(GFX_BOTTOM)` 会把下屏切成 RGB565 单缓冲,退出后与 GUI 的
BGR8 双缓冲冲突 —— 另一个后台缓冲里还留着控制台内容,两帧交替就是闪烁
(封面上传那几帧尤其明显,因为同步 GPU 传输改变了帧节奏)。
citro2d 自绘零格式切换,顺带能显示中文、支持触摸拖动滚动 + 滚动条 + 双击退出。

**输出限定 ASCII** 是因为动态内容(视频标题、接口返回的中文错误)
经 `ui_log_ascii()` 过滤,非 ASCII 合并成 `?`。上屏有完整中文。

### 调试台里能看到什么

设置页点"调试台"进入,会看到:

```
nav isLogin=true sess=yes          登录态与 cookie
bvid=BV1xx cid=12345 qn=32         取流参数
cdn: https://upos-xxx.bilivideo.com  选中的 CDN
danmaku: 577 items                 弹幕条数
tbase: fmt=0ms audio=0ms video=0ms 各流时间基准(排查弹幕整体偏移)
clock sync to 0ms                  播放时钟按首个音频帧 pts 校准的结果
video 640x360 -> 400x224           视频尺寸与显示尺寸
tex 1024x512                       纹理尺寸
mvd level=30 workbuf=4096KB        MVD 工作缓冲
decoder: MVD (hardware)            实际使用的解码器
proc r=00017003 got=1              MVD 每包的处理结果
mvdstdInit=00000000                跳转时 MVD 关掉重开(正常现象)
api code=-xxx ?????              接口错误(中文 msg 显示为 ?,上屏有完整中文)
```

### 常见故障对照

| 现象 | 可能原因 |
|---|---|
| 列表加载失败 | Wi-Fi 未连接;上屏会显示具体错误码 |
| 搜索结果点开"无法播放" | 三个 cid 接口全被拒;条目保留,调试台有每级的 URL 和错误码 |
| 调试台见 webview cid failed | 正常,web 接口被风控拒了,自动走 pagelist/App 接口兜底 |
| 连续大量 http error | httpc 服务态被打坏,现已自动重建会话(`httpc reset...`) |
| 有画面无声音 | 缺 `sdmc:/3ds/dspfirm.cdc`。Rosalina(L+下+SELECT)→ Miscellaneous options → Dump DSP firmware |
| 画面卡顿严重 | 老 3DS 软解性能不足;或网络带宽不够(看是否频繁进缓冲态) |
| 播放几秒后卡住 | CDN 断流,看日志是否有 `net stall, retry`(会自动重连) |
| 拖动进度条后音画不同步 | 已修复(见 seek 章节的 `ndspChnReset` 与 pts 校准) |
| 多次拖动后崩溃 | 已修复(队列满时的槽位覆盖导致重复释放) |
| 拖动多次后越来越不同步 | 已修复(MVD 时间戳队列错位 + swr 未复位,见 seek 章节) |
| 拖动几次后黑屏定格 | 已修复(坏时间戳让帧永远等不到上屏时机) |
| 拖动几次后整机死机(mvd 进程异常) | 已修复(不再"排空"MVD,seek 时关掉重开) |
| seek 后旧画面闪一下 | 已修复(seek 代数校验,旧代帧不上屏) |
| 开播前显示"载入弹幕…" | 正在等弹幕就绪(最多 20 秒),为的是一条都不漏 |
| 起播头几秒弹幕一顿一顿 | 已修复(时钟单调化 + 主线程平滑,见弹幕章节) |
| 弹幕迟到后糊成一片 | 已修复(提前下载 + 起播等就绪 + 行号入场时择空分配) |
| 取流失败 | 视频需要大会员/登录;或 CDN 全是 P2P 节点 |
| 中文显示成 ? | 系统字体覆盖不全,自备 `romfs/font.bcfnt` |
| 编译报路径错误 | 工程路径含空格 |

### 真机断点调试(高级)

Luma3DS 自带 GDB 服务器:主机上 L+↓+SELECT 打开 Rosalina → Debugger →
启用;电脑上用 `arm-none-eabi-gdb` 通过 Wi-Fi 连接,可在真机上下断点、看变量。
比反复看 crash dump 高效得多。

### 关于模拟器

Azahar(Citra 继任者)适合调 UI 和纯逻辑,迭代快。但**不要用它验证**
网络、MVD 硬解、DSP 音频——这些要么没实现(模拟器无 mvd:STD 服务),
要么行为与真机不一致,容易误导排查方向。

---
